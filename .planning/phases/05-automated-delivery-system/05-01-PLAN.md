---
phase: 05-automated-delivery-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/emailer.py
  - app/schemas/delivery.py
  - app/config.py
autonomous: true

must_haves:
  truths:
    - "GraphEmailService sends HTML email via Microsoft Graph API"
    - "Recipient lists are configurable per role via .env variables"
    - "Service gracefully handles missing Graph configuration"
  artifacts:
    - path: "app/services/emailer.py"
      provides: "GraphEmailService with send_email and health_check_async"
      exports: ["GraphEmailService"]
    - path: "app/schemas/delivery.py"
      provides: "EmailRecipients and DeliveryStatus schemas"
      exports: ["EmailRecipients", "DeliveryStatus"]
    - path: "app/config.py"
      provides: "Recipient config per role with TO/CC/BCC"
      contains: "get_email_recipients"
  key_links:
    - from: "app/services/emailer.py"
      to: "app/config.py"
      via: "get_settings() for Graph credentials"
      pattern: "get_settings\\(\\)"
    - from: "app/services/emailer.py"
      to: "app/schemas/delivery.py"
      via: "EmailRecipients for recipient structure"
      pattern: "EmailRecipients"
    - from: "app/config.py"
      to: "app/schemas/delivery.py"
      via: "get_email_recipients returns EmailRecipients"
      pattern: "def get_email_recipients"
---

<objective>
Create Microsoft Graph email service, delivery schemas, and recipient configuration for per-role email delivery.

Purpose: Provides the email sending infrastructure that the pipeline will use to deliver separate intelligence briefs per role (Brokers, Leadership, Compliance, Underwriting).
Output: Working GraphEmailService, EmailRecipients schema, and Settings.get_email_recipients() method.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\BrasilIntel\mdinsights\.planning\PROJECT.md
@C:\BrasilIntel\mdinsights\.planning\ROADMAP.md
@C:\BrasilIntel\mdinsights\.planning\STATE.md
@C:\BrasilIntel\mdinsights\.planning\phases\05-automated-delivery-system\05-RESEARCH.md

# Reference implementation (BrasilIntel's working email service)
@C:\BrasilIntel\app\services\emailer.py
@C:\BrasilIntel\app\schemas\delivery.py

# Current MDInsights config (has Graph fields, needs recipient config)
@C:\BrasilIntel\mdinsights\app\config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create delivery schema and GraphEmailService</name>
  <files>app/schemas/delivery.py, app/services/emailer.py</files>
  <action>
1. Create `app/schemas/delivery.py` modeled on BrasilIntel's `C:\BrasilIntel\app\schemas\delivery.py`:
   - `DeliveryStatus` enum: PENDING, SENT, FAILED, SKIPPED
   - `EmailRecipients` Pydantic model with `to: list[str]`, `cc: list[str] = []`, `bcc: list[str] = []`
   - `has_recipients` property returning `len(self.to) > 0`
   - `total_recipients` property returning sum of all lists

2. Create `app/services/emailer.py` modeled on BrasilIntel's `C:\BrasilIntel\app\services\emailer.py`:
   - `GraphEmailService` class
   - `__init__`: Check `settings.is_graph_configured()`. If true, create `ClientSecretCredential` with `settings.microsoft_tenant_id`, `settings.microsoft_client_id`, `settings.microsoft_client_secret`. Set `self.sender_email = settings.sender_email`. If false, set credential and sender to None.
   - `async def send_email(self, to_addresses, subject, html_body, cc_addresses=None, bcc_addresses=None) -> dict`: Get token via `self.credential.get_token("https://graph.microsoft.com/.default")`, POST to `https://graph.microsoft.com/v1.0/users/{self.sender_email}/sendMail` using `httpx.AsyncClient`, return `{"status": "ok", "recipients": N}` on 202, or `{"status": "error", "message": ...}` on failure. Include 30s timeout.
   - `async def health_check_async(self) -> dict`: GET user info from Graph API to validate connectivity, return status dict.
   - Use `structlog` for logging (consistent with rest of MDInsights, NOT stdlib logging like BrasilIntel).
   - Import from `app.config import get_settings` (MDInsights pattern, NOT BrasilIntel's import path).

Key differences from BrasilIntel reference:
- Use `structlog.get_logger` not `logging.getLogger` (MDInsights convention)
- Use `settings.microsoft_tenant_id` not `settings.azure_tenant_id` (MDInsights field names from config.py)
- Do NOT include `send_email_with_attachment` or `send_report_email_with_pdf` (not needed for MDInsights)
- Do NOT include `send_report_email` (pipeline will call send_email directly)
  </action>
  <verify>
    Run `python -c "from app.services.emailer import GraphEmailService; from app.schemas.delivery import EmailRecipients, DeliveryStatus; print('imports OK')"` from C:\BrasilIntel\mdinsights.
  </verify>
  <done>GraphEmailService class exists with send_email and health_check_async methods. EmailRecipients and DeliveryStatus schemas importable.</done>
</task>

<task type="auto">
  <name>Task 2: Add recipient configuration to Settings</name>
  <files>app/config.py</files>
  <action>
Read the current `app/config.py` and add recipient configuration fields to the Settings class:

1. Add 12 recipient fields (4 roles x 3 types: to/cc/bcc) as optional strings with empty defaults:
   ```
   report_recipients_brokers: str = ""
   report_recipients_brokers_cc: str = ""
   report_recipients_brokers_bcc: str = ""
   report_recipients_leadership: str = ""
   report_recipients_leadership_cc: str = ""
   report_recipients_leadership_bcc: str = ""
   report_recipients_compliance: str = ""
   report_recipients_compliance_cc: str = ""
   report_recipients_compliance_bcc: str = ""
   report_recipients_underwriting: str = ""
   report_recipients_underwriting_cc: str = ""
   report_recipients_underwriting_bcc: str = ""
   ```

2. Add `admin_email: str = ""` for failure alerting.

3. Add private helper `_parse_recipient_list(self, recipients_str: str) -> list[str]`:
   - If empty string, return `[]`
   - Split by comma, strip whitespace, filter empty strings

4. Add `get_email_recipients(self, role: str) -> EmailRecipients`:
   - Map role names ("Brokers", "Leadership", "Compliance", "Underwriting") to their field name prefixes
   - Use `getattr(self, field_name, "")` to get each field value
   - Parse with `_parse_recipient_list`
   - Return `EmailRecipients(to=..., cc=..., bcc=...)`
   - Return empty EmailRecipients for unknown roles

5. Add import for `EmailRecipients` from `app.schemas.delivery`.

Follow the exact pattern from the research document (Pattern 5: Recipient Management).
  </action>
  <verify>
    Run `python -c "from app.config import get_settings; s = get_settings(); r = s.get_email_recipients('Brokers'); print(f'has_recipients={r.has_recipients}, to={r.to}')"` from C:\BrasilIntel\mdinsights. Should print `has_recipients=False, to=[]` (no .env config yet).
  </verify>
  <done>Settings has recipient fields for all 4 roles, admin_email field, _parse_recipient_list helper, and get_email_recipients method returning EmailRecipients objects.</done>
</task>

</tasks>

<verification>
1. `python -c "from app.services.emailer import GraphEmailService; print('emailer OK')"` -- passes
2. `python -c "from app.schemas.delivery import EmailRecipients, DeliveryStatus; print('schema OK')"` -- passes
3. `python -c "from app.config import get_settings; s = get_settings(); print(s.get_email_recipients('Brokers'))"` -- returns EmailRecipients(to=[], cc=[], bcc=[])
4. `python -c "from app.config import get_settings; print(get_settings().admin_email)"` -- returns empty string
</verification>

<success_criteria>
- GraphEmailService importable and initializes without error (Graph not configured returns graceful fallback)
- EmailRecipients schema validates TO/CC/BCC lists correctly
- Settings.get_email_recipients() returns proper EmailRecipients for all 4 roles
- All existing MDInsights code continues to work (no import breaks)
</success_criteria>

<output>
After completion, create `.planning/phases/05-automated-delivery-system/05-01-SUMMARY.md`
</output>
