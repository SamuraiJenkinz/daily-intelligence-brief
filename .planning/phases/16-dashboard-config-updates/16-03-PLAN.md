---
phase: 16-dashboard-config-updates
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - app/schemas/admin.py
  - app/routers/admin.py
  - app/templates/admin/factiva.html
autonomous: true

must_haves:
  truths:
    - "Source creation works without source_type or actor_id fields"
    - "Source update works without source_type or actor_id fields"
    - "SourceCreate and SourceUpdate schemas no longer require source_type"
    - "FactivaConfig page has header note explaining sole-source role"
    - "FactivaConfig disable warning clearly states no fallback exists"
    - "FactivaConfig page shows date_range_hours field with help text"
    - "Saving FactivaConfig persists date_range_hours value"
  artifacts:
    - path: "app/schemas/admin.py"
      provides: "Simplified SourceCreate/SourceUpdate without source_type or actor_id"
    - path: "app/routers/admin.py"
      provides: "Source routes without source_type/actor_id params, Factiva route with date_range_hours"
    - path: "app/templates/admin/factiva.html"
      provides: "FactivaConfig page with sole-source header, clear disable warning, date_range_hours field"
      contains: "sole news collection source"
  key_links:
    - from: "app/schemas/admin.py (SourceCreate)"
      to: "app/routers/admin.py (create_source)"
      via: "Pydantic validation in route handler"
      pattern: "SourceCreate\\(name=name"
    - from: "app/routers/admin.py (update_factiva_config)"
      to: "app/templates/admin/factiva.html"
      via: "date_range_hours form field"
      pattern: "date_range_hours"
    - from: "app/templates/admin/factiva.html"
      to: "app/routers/admin.py (update_factiva_config)"
      via: "POST /admin/factiva form submission"
      pattern: "name=\"date_range_hours\""
---

<objective>
Update source route handlers and schemas to match simplified UI, and enhance FactivaConfig page to clearly communicate its sole-source role with helpful guidance.

Purpose: Backend routes and schemas must align with the simplified source forms (no type/actor_id). FactivaConfig page must feel like THE collection control center with clear warnings about disabling collection and helpful documentation for each field.

Output: Working source CRUD without type/actor_id, enhanced FactivaConfig page with header note, clear disable warning, and date_range_hours field.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-dashboard-config-updates/16-CONTEXT.md
@.planning/phases/16-dashboard-config-updates/16-RESEARCH.md
@.planning/phases/16-dashboard-config-updates/16-01-SUMMARY.md
@.planning/phases/16-dashboard-config-updates/16-02-SUMMARY.md
@app/schemas/admin.py
@app/routers/admin.py
@app/templates/admin/factiva.html
@app/models/source.py
@app/models/factiva_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update source schemas and route handlers for simplified source model</name>
  <files>
    app/schemas/admin.py
    app/routers/admin.py
  </files>
  <action>
    In app/schemas/admin.py:
    - Remove `source_type` field and its validator from SourceCreate. Remove `actor_id` field. Keep name, url, enabled.
    - Remove `source_type` field and its validator from SourceUpdate. Remove `actor_id` field. Keep name, url, enabled.
    - Remove the `Literal` import if no longer used.
    - Update class docstrings to reflect simplified schema.

    Resulting SourceCreate:
    ```python
    class SourceCreate(BaseModel):
        """Schema for creating a new source."""
        name: str
        url: str
        enabled: bool = True

        @field_validator('name')
        @classmethod
        def validate_name(cls, v: str) -> str:
            """Validate name is not empty and within length limits."""
            if not v or len(v.strip()) == 0:
                raise ValueError('Name cannot be empty')
            if len(v) > 255:
                raise ValueError('Name must be 255 characters or less')
            return v.strip()

        @field_validator('url')
        @classmethod
        def validate_url(cls, v: str) -> str:
            """Validate URL is not empty."""
            if not v or len(v.strip()) == 0:
                raise ValueError('URL cannot be empty')
            return v.strip()
    ```

    SourceUpdate should mirror SourceCreate (same fields and validators).

    In app/routers/admin.py `create_source()` (around line 539):
    - Remove `source_type: str = Form(...)` parameter.
    - Remove `actor_id: Optional[str] = Form(None)` parameter.
    - Update SourceCreate() call to only pass name, url, enabled.
    - Remove source_type and actor_id from error form_data dicts.
    - When creating the Source ORM object, use `source_type=SourceType.RSS` as a default (DB column is NOT NULL, so must have a value — RSS is a safe default since it's the non-Apify option). Set `actor_id=None`.
    - Keep the SourceType import since it's still needed for the default assignment.

    In app/routers/admin.py `update_source()` (around line 652):
    - Remove `source_type: str = Form(...)` parameter.
    - Remove `actor_id: Optional[str] = Form(None)` parameter.
    - Update SourceUpdate() call to only pass name, url, enabled.
    - When updating the Source ORM object, do NOT modify source.source_type or source.actor_id (preserve existing DB values).
    - Remove source_type/actor_id from error form_data if present.

    In app/routers/admin.py, remove the `from app.schemas.admin import SourceCreate, SourceUpdate` import if it was changed — but it should still work since we just removed fields.

    Do NOT modify the Source ORM model or SourceType enum (DB schema preservation per Phase 15-03 decision).
  </action>
  <verify>
    Run: `python -c "from app.schemas.admin import SourceCreate, SourceUpdate; sc = SourceCreate(name='Test', url='https://test.com'); print('OK:', sc.name)"` — should succeed without source_type.
    Run: `grep -n "source_type" app/schemas/admin.py` — should return 0 lines.
    Run: `grep -n "actor_id" app/schemas/admin.py` — should return 0 lines.
    Run: `grep -c "source_type.*Form" app/routers/admin.py` — should return 0 (no source_type Form params in route handlers).
    Run: `grep -c "actor_id.*Form" app/routers/admin.py` — should return 0 (no actor_id Form params in route handlers).
  </verify>
  <done>
    SourceCreate and SourceUpdate schemas accept only name, url, enabled. Route handlers create/update sources without requiring type or actor_id from the UI. Existing DB values for source_type and actor_id are preserved on update.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance FactivaConfig page with sole-source messaging and date_range_hours field</name>
  <files>
    app/templates/admin/factiva.html
    app/routers/admin.py
  </files>
  <action>
    In app/templates/admin/factiva.html:

    1. ADD HEADER NOTE (after the existing header, around line 52-53):
       After the `<p class="text-muted mb-0">` paragraph, add:
       ```html
       <div class="alert alert-info py-2 mb-0 mt-2">
           <i class="bi bi-info-circle me-2"></i>
           <strong>Sole Collection Source:</strong> Factiva is the only news collection source for MDInsights. All articles are collected via the MMC Core API.
       </div>
       ```

    2. ADD DATE_RANGE_HOURS FIELD (after the page_size field, around line 143, before the enabled checkbox):
       ```html
       <div class="mb-3">
           <label for="date_range_hours" class="form-label fw-semibold">Date Range (Hours)</label>
           <input
               type="number"
               id="date_range_hours"
               name="date_range_hours"
               class="form-control"
               style="max-width: 200px;"
               value="{{ config.date_range_hours or 48 }}"
               min="1"
               max="168"
           >
           <div class="field-hint">
               How far back to look for articles each collection run. Default 48 hours provides overlap between runs to catch late-indexed articles.
           </div>
       </div>
       ```

    3. REPLACE MISLEADING FALLBACK HINT (line 161-163):
       Replace:
       ```html
       <div class="field-hint ms-4">
           When disabled, the pipeline automatically falls back to Apify/RSS collection.
       </div>
       ```
       With:
       ```html
       <div class="field-hint ms-4 text-danger">
           <i class="bi bi-exclamation-triangle-fill me-1"></i>
           <strong>Warning:</strong> Disabling Factiva will stop all news collection. No fallback source is available.
       </div>
       ```

    In app/routers/admin.py `update_factiva_config()` (around line 1284):
    - Add `date_range_hours: int = Form(48)` parameter.
    - Add validation: clamp date_range_hours between 1 and 168 (1 hour to 7 days).
    - Set `config.date_range_hours = date_range_hours` alongside the other field updates (around line 1331).
    - Add date_range_hours to the logger.info call.
  </action>
  <verify>
    Run: `grep "Sole Collection Source" app/templates/admin/factiva.html` — should match the header note.
    Run: `grep "stop all news collection" app/templates/admin/factiva.html` — should match the disable warning.
    Run: `grep "date_range_hours" app/templates/admin/factiva.html` — should match the new field.
    Run: `grep "date_range_hours" app/routers/admin.py` — should match the route handler parameter and assignment.
    Run: `grep -c "Apify" app/templates/admin/factiva.html` — should return 0.
  </verify>
  <done>
    FactivaConfig page has: (1) Header note explaining sole-source role, (2) date_range_hours field with help text, (3) clear warning that disabling stops all collection. POST handler persists date_range_hours. No Apify references remain.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from app.schemas.admin import SourceCreate; sc = SourceCreate(name='Test', url='https://x.com'); print(sc)"` succeeds
2. `grep -rn "Apify" app/templates/admin/factiva.html` returns no matches
3. `grep -rn "Apify" app/schemas/admin.py` returns no matches
4. `grep "date_range_hours" app/routers/admin.py` shows param and assignment
5. `grep "Sole Collection Source" app/templates/admin/factiva.html` matches header note
6. `grep "stop all news collection" app/templates/admin/factiva.html` matches warning
</verification>

<success_criteria>
- Source create/update works with only name, url, enabled (no source_type or actor_id required)
- FactivaConfig page clearly identifies itself as the sole collection configuration
- Disable warning explicitly states no fallback is available
- date_range_hours is editable in the admin UI and persists on save
- Zero Apify references in schemas/admin.py and factiva.html
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-config-updates/16-03-SUMMARY.md`
</output>
