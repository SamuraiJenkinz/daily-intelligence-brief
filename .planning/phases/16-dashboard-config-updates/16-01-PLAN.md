---
phase: 16-dashboard-config-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/admin.py
  - app/models/news_article.py
  - app/main.py
  - .env.example
  - app/templates/admin/dashboard.html
autonomous: true

must_haves:
  truths:
    - "Dashboard news API status shows healthy or offline only (no degraded state for news)"
    - "Fallback event log does not query or display NEWS_FALLBACK events"
    - "Run source breakdown on dashboard shows Factiva badge only (no Apify/RSS badge)"
    - "New articles default to 'Factiva' collector_source in both model and migration SQL"
    - ".env.example documents Factiva as the sole news collection source"
  artifacts:
    - path: "app/routers/admin.py"
      provides: "Status calculation without NEWS_FALLBACK in FALLBACK_TYPES or FALLBACK_EVENT_TYPES"
      contains: "EQUITY_FALLBACK"
    - path: "app/models/news_article.py"
      provides: "NewsArticle model with Factiva default"
      contains: "default=\"Factiva\""
    - path: "app/main.py"
      provides: "Startup migration with Factiva default"
      contains: "DEFAULT 'Factiva'"
    - path: "app/templates/admin/dashboard.html"
      provides: "Dashboard without Apify/RSS badge rendering"
    - path: ".env.example"
      provides: "Config template documenting Factiva-only architecture"
  key_links:
    - from: "app/routers/admin.py (_get_enterprise_api_status)"
      to: "app/templates/admin/dashboard.html"
      via: "enterprise_status template variable"
      pattern: "FALLBACK_TYPES.*EQUITY_FALLBACK"
    - from: "app/routers/admin.py (_get_fallback_events)"
      to: "app/templates/admin/dashboard.html"
      via: "fallback_events template variable"
      pattern: "FALLBACK_EVENT_TYPES.*EQUITY_FALLBACK"
---

<objective>
Update health monitoring, model defaults, and dashboard to reflect Factiva-only collection architecture.

Purpose: Remove the "degraded" news API state (no fallback exists), stop querying NEWS_FALLBACK events, simplify the run source breakdown to Factiva-only, update model and migration defaults from 'Apify/RSS' to 'Factiva', and document the architecture in .env.example.

Output: Backend status logic, model defaults, migration SQL, dashboard template, and .env.example all reflect single-source (Factiva) reality.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-dashboard-config-updates/16-CONTEXT.md
@.planning/phases/16-dashboard-config-updates/16-RESEARCH.md
@app/routers/admin.py
@app/models/news_article.py
@app/models/api_event.py
@app/main.py
@app/templates/admin/dashboard.html
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove NEWS_FALLBACK from status logic and update model defaults</name>
  <files>
    app/routers/admin.py
    app/models/news_article.py
    app/main.py
    .env.example
  </files>
  <action>
    In app/routers/admin.py `_get_enterprise_api_status()` (around line 71-75):
    - Remove `ApiEventType.NEWS_FALLBACK` from the `FALLBACK_TYPES` set. Keep EQUITY_FALLBACK and EMAIL_FALLBACK.
    - This means news API events where success=False will always map to "offline" (not "degraded"), which is correct since no news fallback exists.

    In app/routers/admin.py `_get_fallback_events()` (around line 122-127):
    - Remove `ApiEventType.NEWS_FALLBACK` from the `FALLBACK_EVENT_TYPES` list. Keep EQUITY_FALLBACK, EMAIL_FALLBACK, and TOKEN_FAILED.
    - This prevents historical NEWS_FALLBACK events from appearing in the fallback event log.

    In app/models/news_article.py (line 39):
    - Change `default="Apify/RSS"` to `default="Factiva"` on the collector_source column.
    - Update the comment on line 38 to remove the "Historical data may contain" note since fresh DB is planned. New comment: `# Source attribution: "Factiva" (sole collection source since v1.2)`

    In app/main.py startup migration (around line 56-58):
    - Change `DEFAULT 'Apify/RSS'` to `DEFAULT 'Factiva'` in the ALTER TABLE SQL string.

    In .env.example, in the MMC CORE API section (around line 117-119):
    - Update the section comment to clarify Factiva is the sole news source. Change:
      `# Required for enterprise API access: Factiva news, equity prices, email delivery`
      to:
      `# Required for enterprise API access: Factiva news (sole collection source), equity prices, email delivery`
    - No other .env.example changes needed (verified: no Apify variables exist in .env.example).
  </action>
  <verify>
    Run: `python -c "from app.models.news_article import NewsArticle; assert NewsArticle.collector_source.default.arg == 'Factiva', 'Default not updated'"`
    Run: `grep -n "NEWS_FALLBACK" app/routers/admin.py` — should return zero lines.
    Run: `grep "DEFAULT 'Factiva'" app/main.py` — should match the migration SQL.
    Run: `grep "sole collection source" .env.example` — should match the updated comment.
  </verify>
  <done>
    NEWS_FALLBACK removed from both status calculation and fallback event query. NewsArticle default is 'Factiva'. Migration SQL default is 'Factiva'. .env.example documents Factiva-only architecture.
  </done>
</task>

<task type="auto">
  <name>Task 2: Simplify dashboard run source breakdown to Factiva-only</name>
  <files>app/templates/admin/dashboard.html</files>
  <action>
    In app/templates/admin/dashboard.html, find the run source breakdown section (around lines 258-271). Replace the multi-source badge rendering with Factiva-only rendering.

    BEFORE (current lines 258-271):
    ```html
    <td>
        {% if run.source_breakdown %}
            {% if run.source_breakdown.get('Factiva') %}
                <span class="badge" style="background-color: #0077c8;">{{ run.source_breakdown['Factiva'] }} Factiva</span>
            {% endif %}
            {% if run.source_breakdown.get('Apify/RSS') %}
                <span class="badge bg-secondary">{{ run.source_breakdown['Apify/RSS'] }} Apify/RSS</span>
            {% endif %}
            {% if not run.source_breakdown.get('Factiva') and not run.source_breakdown.get('Apify/RSS') %}
                <span class="text-muted">-</span>
            {% endif %}
        {% else %}
            <span class="text-muted">-</span>
        {% endif %}
    </td>
    ```

    AFTER:
    ```html
    <td>
        {% if run.source_breakdown and run.source_breakdown.get('Factiva') %}
            <span class="badge" style="background-color: #0077c8;">
                <i class="bi bi-newspaper me-1"></i>{{ run.source_breakdown['Factiva'] }} Factiva
            </span>
        {% else %}
            <span class="text-muted">-</span>
        {% endif %}
    </td>
    ```

    This removes the Apify/RSS badge path entirely and adds the newspaper icon to the Factiva badge for consistency with other Factiva badges in the app.
  </action>
  <verify>
    Run: `grep -c "Apify" app/templates/admin/dashboard.html` — should return 0.
    Run: `grep "bi-newspaper" app/templates/admin/dashboard.html` — should match the Factiva badge.
  </verify>
  <done>
    Dashboard run source breakdown renders Factiva badge only with newspaper icon. No Apify/RSS references remain in dashboard.html.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "NEWS_FALLBACK" app/routers/admin.py` returns no matches
2. `grep -rn "Apify" app/templates/admin/dashboard.html` returns no matches
3. `grep "default.*Factiva" app/models/news_article.py` confirms Factiva default
4. `grep "DEFAULT 'Factiva'" app/main.py` confirms migration SQL default
5. `python -c "from app.routers.admin import _get_enterprise_api_status"` imports without error
</verification>

<success_criteria>
- News API health shows binary healthy/offline (no degraded state possible for news)
- Fallback event log excludes NEWS_FALLBACK events
- Dashboard run breakdown shows Factiva badge only
- NewsArticle model and migration SQL both default to 'Factiva'
- .env.example documents Factiva as sole news collection source
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-config-updates/16-01-SUMMARY.md`
</output>
