---
phase: 16-dashboard-config-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/role_brief.html
  - app/templates/email/role_email.html
  - app/templates/admin/partials/search_results.html
  - app/templates/admin/partials/source_form.html
  - app/templates/admin/partials/source_edit_row.html
  - app/templates/admin/partials/source_row.html
  - app/templates/admin/sources.html
autonomous: true

must_haves:
  truths:
    - "Brief template renders Factiva badge for all articles (no Apify/RSS badge path)"
    - "Email template renders Factiva badge for all articles (no Apify/RSS badge path)"
    - "Search results render Factiva badge only (no secondary badge for non-Factiva sources)"
    - "Source form has no type dropdown or actor_id field"
    - "Source edit row has no type dropdown or actor_id field"
    - "Source list table has no Type or Actor ID columns"
  artifacts:
    - path: "app/templates/role_brief.html"
      provides: "Brief template with Factiva-only badge"
    - path: "app/templates/email/role_email.html"
      provides: "Email template with Factiva-only badge"
    - path: "app/templates/admin/partials/search_results.html"
      provides: "Search results with Factiva-only badge"
    - path: "app/templates/admin/partials/source_form.html"
      provides: "Source create form without type or actor_id"
    - path: "app/templates/admin/partials/source_edit_row.html"
      provides: "Source edit row without type or actor_id"
    - path: "app/templates/admin/partials/source_row.html"
      provides: "Source display row without type or actor_id columns"
    - path: "app/templates/admin/sources.html"
      provides: "Sources table without Type or Actor ID headers"
  key_links:
    - from: "app/templates/admin/partials/source_form.html"
      to: "app/routers/admin.py (create_source)"
      via: "HTMX hx-post /admin/sources/create"
      pattern: "hx-post.*sources/create"
    - from: "app/templates/admin/partials/source_edit_row.html"
      to: "app/routers/admin.py (update_source)"
      via: "HTMX hx-post /admin/sources/{id}"
      pattern: "hx-post.*sources/"
---

<objective>
Remove Apify/RSS rendering from all templates and simplify source management UI by removing type and actor_id fields.

Purpose: All article badge rendering should show Factiva only (fresh DB, no historical Apify data). Source management UI should reflect the reality that source type and actor_id are legacy concepts no longer needed.

Output: Clean templates with Factiva-only badges, simplified source forms without type/actor_id fields.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-dashboard-config-updates/16-CONTEXT.md
@.planning/phases/16-dashboard-config-updates/16-RESEARCH.md
@app/templates/role_brief.html
@app/templates/email/role_email.html
@app/templates/admin/partials/search_results.html
@app/templates/admin/partials/source_form.html
@app/templates/admin/partials/source_edit_row.html
@app/templates/admin/partials/source_row.html
@app/templates/admin/sources.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Apify/RSS badge rendering from brief, email, and search templates</name>
  <files>
    app/templates/role_brief.html
    app/templates/email/role_email.html
    app/templates/admin/partials/search_results.html
  </files>
  <action>
    In app/templates/role_brief.html (around lines 740-744):
    Replace the if/else conditional that shows "via Factiva" or "via Apify/RSS" with a single Factiva badge that always renders when collector_source exists:

    BEFORE:
    ```html
    {% if article.collector_source == 'Factiva' %}
    <span style="display: inline-block; background-color: #0077c8; color: white; font-size: 0.7em; padding: 2px 8px; border-radius: 10px; margin-left: 8px; font-weight: 600;">via Factiva</span>
    {% else %}
    <span style="display: inline-block; background-color: #6c757d; color: white; font-size: 0.7em; padding: 2px 8px; border-radius: 10px; margin-left: 8px; font-weight: 600;">via Apify/RSS</span>
    {% endif %}
    ```

    AFTER:
    ```html
    {% if article.collector_source %}
    <span style="display: inline-block; background-color: #0077c8; color: white; font-size: 0.7em; padding: 2px 8px; border-radius: 10px; margin-left: 8px; font-weight: 600;">via Factiva</span>
    {% endif %}
    ```

    In app/templates/email/role_email.html (around lines 137-141):
    Same pattern — replace the if/else with a single Factiva badge:

    BEFORE:
    ```html
    {% if article.collector_source == 'Factiva' %}
    <span style="display: inline-block; background-color: #0077c8; color: #ffffff; font-size: 10px; padding: 1px 6px; border-radius: 8px; margin-left: 6px; font-weight: 600;">via Factiva</span>
    {% else %}
    <span style="display: inline-block; background-color: #6c757d; color: #ffffff; font-size: 10px; padding: 1px 6px; border-radius: 8px; margin-left: 6px; font-weight: 600;">via Apify/RSS</span>
    {% endif %}
    ```

    AFTER:
    ```html
    {% if article.collector_source %}
    <span style="display: inline-block; background-color: #0077c8; color: #ffffff; font-size: 10px; padding: 1px 6px; border-radius: 8px; margin-left: 6px; font-weight: 600;">via Factiva</span>
    {% endif %}
    ```

    In app/templates/admin/partials/search_results.html (around lines 35-43):
    Replace the if/elif conditional with a single Factiva badge:

    BEFORE:
    ```html
    {% if article.collector_source == 'Factiva' %}
    <span class="badge me-3" style="background-color: #0077c8; font-size: 0.7rem;">
        <i class="bi bi-newspaper me-1"></i>Factiva
    </span>
    {% elif article.collector_source %}
    <span class="badge bg-secondary me-3" style="font-size: 0.7rem;">
        <i class="bi bi-rss me-1"></i>{{ article.collector_source }}
    </span>
    {% endif %}
    ```

    AFTER:
    ```html
    {% if article.collector_source %}
    <span class="badge me-3" style="background-color: #0077c8; font-size: 0.7rem;">
        <i class="bi bi-newspaper me-1"></i>Factiva
    </span>
    {% endif %}
    ```
  </action>
  <verify>
    Run: `grep -c "Apify" app/templates/role_brief.html` — should return 0.
    Run: `grep -c "Apify" app/templates/email/role_email.html` — should return 0.
    Run: `grep -c "rss" app/templates/admin/partials/search_results.html` — should return 0.
  </verify>
  <done>
    All three templates render Factiva badge only. No Apify/RSS rendering paths remain in brief, email, or search result templates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Simplify source management UI (remove type and actor_id)</name>
  <files>
    app/templates/admin/partials/source_form.html
    app/templates/admin/partials/source_edit_row.html
    app/templates/admin/partials/source_row.html
    app/templates/admin/sources.html
  </files>
  <action>
    In app/templates/admin/partials/source_form.html:
    - Delete the entire Source Type dropdown block (lines 38-53, the div.col-md-4 containing the select for newSourceType).
    - Delete the entire Actor ID field block (lines 55-65, the div.col-md-4 containing the newSourceActorId input).
    - Expand the Status column from col-md-4 to col-md-6 to fill the freed space. Or keep the layout clean by making Name and URL each col-md-6 (already are) and putting Status on a new row. Keep Status in the same row but change its col class to col-md-12 or col-md-6.

    Resulting form layout: Name (col-md-6), URL (col-md-6) on row 1. Status toggle (col-md-6) on row 2 alongside the buttons.

    In app/templates/admin/partials/source_edit_row.html:
    - Remove the Type dropdown cell (lines 26-37, the `<td>` containing the source_type select).
    - Remove the Actor ID input cell (lines 39-46, the `<td>` with the actor_id input). Note: keep the `<td>` for enabled and actions which follow.
    - The row should have cells: Name, URL, Status, Actions (4 cells matching the updated table headers).

    In app/templates/admin/partials/source_row.html:
    - Remove the Type badge cell (lines 11-16, the `<td>` with Apify/RSS badges).
    - Remove the Actor ID cell (lines 17-23, the `<td>` with actor_id code display).
    - The row should have cells: Name, URL, Status, Actions (4 cells matching the updated table headers).

    In app/templates/admin/sources.html:
    - Remove the "Type" and "Actor ID" `<th>` headers from the table (lines 115-116).
    - Table headers should be: Name, URL, Status, Actions.
    - Remove the `.badge-type` CSS class from the `<style>` block (lines 27-30) since type badges are no longer rendered.
  </action>
  <verify>
    Run: `grep -ci "source_type\|actor_id\|apify\|newSourceType\|newSourceActorId" app/templates/admin/partials/source_form.html` — should return 0.
    Run: `grep -ci "source_type\|actor_id\|apify" app/templates/admin/partials/source_edit_row.html` — should return 0.
    Run: `grep -ci "source_type\|actor_id\|apify\|rss" app/templates/admin/partials/source_row.html` — should return 0.
    Run: `grep -c "Type\|Actor ID" app/templates/admin/sources.html | head -1` — "Type" should only appear in "Source Type" CSS if at all, not as table header.
  </verify>
  <done>
    Source form no longer has type dropdown or actor_id field. Edit row, display row, and table headers all reflect simplified 4-column layout (Name, URL, Status, Actions). No Apify/RSS references remain in source management templates.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "Apify\|apify" app/templates/` returns NO matches in any template file
2. `grep -rn "actor_id" app/templates/admin/partials/source_form.html app/templates/admin/partials/source_edit_row.html` returns no matches
3. `grep -rn "source_type" app/templates/admin/partials/source_form.html app/templates/admin/partials/source_edit_row.html` returns no matches
4. Source table headers are Name, URL, Status, Actions (4 columns)
</verification>

<success_criteria>
- Zero Apify/RSS references in any template file under app/templates/
- Brief and email templates render Factiva badge for all articles
- Source forms have only Name, URL, and Status fields
- Source table shows only Name, URL, Status, and Actions columns
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-config-updates/16-02-SUMMARY.md`
</output>
