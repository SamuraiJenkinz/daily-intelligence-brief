---
phase: 20-archive-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/audio_generator.py
  - app/services/pipeline.py
  - app/models/api_event.py
autonomous: true

must_haves:
  truths:
    - "TTS events logged to api_events include character_count and role in the detail JSON"
    - "Audio files older than AUDIO_RETENTION_DAYS are automatically deleted during pipeline execution"
    - "Empty date directories are removed after cleanup"
    - "Cleanup summary is logged to api_events with AUDIO_CLEANUP event type"
  artifacts:
    - path: "app/services/audio_generator.py"
      provides: "Character count and role in TTS event detail JSON"
      contains: "character_count"
    - path: "app/services/pipeline.py"
      provides: "Audio cleanup step after Step 9"
      contains: "AUDIO_RETENTION_DAYS"
    - path: "app/models/api_event.py"
      provides: "AUDIO_CLEANUP event type"
      contains: "AUDIO_CLEANUP"
  key_links:
    - from: "app/services/pipeline.py"
      to: "data/audio/{date}/*.mp3"
      via: "pathlib mtime-based age calculation"
      pattern: "st_mtime"
    - from: "app/services/pipeline.py"
      to: "app/models/api_event.py"
      via: "AUDIO_CLEANUP event logging"
      pattern: "AUDIO_CLEANUP"
---

<objective>
Add TTS character count tracking to api_events and automated audio file retention cleanup to the daily pipeline.

Purpose: OPS-03 requires character usage tracking per role per day for cost monitoring. OPS-04 requires automatic cleanup of audio files past a configurable retention period. This plan adds the backend data and cleanup logic that the cost monitoring dashboard (Plan 20-03) will display.

Output: Enhanced TTS event logging with character_count + role in detail JSON, new AUDIO_CLEANUP event type, and a cleanup step in the pipeline that deletes audio files older than AUDIO_RETENTION_DAYS (default 90).
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-archive-operations/20-RESEARCH.md

Key facts from prior phases:
- audio_generator.py `_log_tts_event()` currently logs detail as `{"provider": "azure", "size_mb": 2.1}` — missing character_count and role
- audio_generator.py `_convert_to_audio()` receives the preprocessed script text (character count = len(script))
- audio_generator.py `generate_briefing()` has access to role name and preprocessed_script
- Pipeline steps go 0-9. Cleanup should be Step 10 (after Step 9: run record update)
- ApiEventType enum already has TTS_SUCCESS and TTS_FALLBACK — need to add AUDIO_CLEANUP
- api_events uses SessionLocal pattern for database access
- audio files stored at data/audio/{YYYY-MM-DD}/{role}.mp3
- AUDIO_DIR constant in audio_generator.py: os.path.join(PROJECT_ROOT, "data", "audio")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add character_count and role to TTS event logging</name>
  <files>app/services/audio_generator.py, app/models/api_event.py</files>
  <action>
  1. In `app/models/api_event.py`, add `AUDIO_CLEANUP = "audio_cleanup"` to the ApiEventType enum, after TTS_FALLBACK. Add a comment: `# Cleanup events (Phase 20):` before it, and document it in the docstring as `AUDIO_CLEANUP - Audio file retention cleanup summary`.

  2. In `app/services/audio_generator.py`, modify `generate_briefing()` to pass character_count and role to `_convert_to_audio()`. Specifically:
     - After Step 3 (preprocessed_script assignment), calculate `character_count = len(preprocessed_script)`
     - Modify the `_convert_to_audio()` call to pass character_count and role as additional params: `self._convert_to_audio(preprocessed_script, output_path, role=role, character_count=character_count)`

  3. Modify `_convert_to_audio()` signature to accept `role: str = ""` and `character_count: int = 0` parameters.

  4. Update both `_log_tts_event()` calls inside `_convert_to_audio()` to include character_count and role in the detail dict:
     - For TTS_SUCCESS: `detail={"size_mb": result["size_mb"], "character_count": character_count, "role": role}`
     - For TTS_FALLBACK: add `"character_count": character_count, "role": role` to the existing detail dict

  Do NOT change the `_log_tts_event()` method signature itself — it already accepts a generic detail dict. The change is only in what we pass to it.
  </action>
  <verify>
  Run: `python -c "from app.models.api_event import ApiEventType; print(ApiEventType.AUDIO_CLEANUP.value)"` — should print "audio_cleanup"
  Run: `grep -n "character_count" app/services/audio_generator.py` — should show character_count in _convert_to_audio params and detail dicts
  Run: `grep -n '"role"' app/services/audio_generator.py` — should show role in detail dicts passed to _log_tts_event
  </verify>
  <done>TTS_SUCCESS and TTS_FALLBACK events in api_events now include character_count and role in their detail JSON. AUDIO_CLEANUP event type exists in enum.</done>
</task>

<task type="auto">
  <name>Task 2: Add audio file retention cleanup to pipeline</name>
  <files>app/services/pipeline.py</files>
  <action>
  1. Add a new method `_cleanup_old_audio_files(self)` to PipelineOrchestrator class. Place it after `_generate_audio_parallel()`. Implementation:
     - Read retention days from environment: `int(os.environ.get("AUDIO_RETENTION_DAYS", "90"))`
     - Use pathlib to scan `data/audio/` directory (use the same path pattern as the streaming endpoint: `Path(__file__).parent.parent.parent / "data" / "audio"`)
     - If audio_dir doesn't exist, log `audio_cleanup_skipped` with reason and return
     - Iterate through date directories, then through *.mp3 files in each
     - For each file, calculate age using `datetime.fromtimestamp(audio_file.stat().st_mtime)`
     - If mtime < cutoff_time (now - retention_days), delete with `audio_file.unlink()`, track deleted count and total bytes
     - After processing each date directory's files, check if directory is empty with `not any(date_dir.iterdir())` and remove with `date_dir.rmdir()`
     - Log per-file deletions to structlog: `audio_file_deleted` with file name, date_dir name, age_days, size_mb
     - Log summary to structlog: `audio_cleanup_complete` with deleted_count, deleted_mb, retention_days
     - Log summary to api_events: Create AUDIO_CLEANUP event with `api_name="tts"`, `success=True`, detail JSON containing `{"deleted_count": N, "deleted_mb": X.XX, "retention_days": 90}`. Use the SessionLocal pattern (import from app.database) wrapped in try/except to never propagate logging errors. Import ApiEvent and ApiEventType at the top of the method (lazy import like _generate_audio_parallel does for AudioBriefingService).
     - Wrap the entire method body in try/except Exception — audio cleanup failure must NEVER crash the pipeline. Log error and return silently.

  2. Integrate cleanup into the pipeline's `run()` method. Add a new step AFTER the pipeline_summary log (after line ~1032, before `return result`):
     ```
     # Step 10: Audio file retention cleanup
     step_start = datetime.utcnow()
     self._cleanup_old_audio_files()
     step_duration = (datetime.utcnow() - step_start).total_seconds()
     self.logger.info("step_10_audio_cleanup", duration_seconds=round(step_duration, 2))
     ```
     This goes AFTER the pipeline_summary log but BEFORE `return result`.

  Important: Only add cleanup to the `run()` method (the production async pipeline), NOT the `run_sync()` method (legacy/test pipeline). The cleanup should run once per day in the production pipeline only.
  </action>
  <verify>
  Run: `grep -n "Step 10" app/services/pipeline.py` — should show step 10 audio cleanup
  Run: `grep -n "_cleanup_old_audio_files" app/services/pipeline.py` — should show method definition and call
  Run: `grep -n "AUDIO_RETENTION_DAYS" app/services/pipeline.py` — should show env var read
  Run: `grep -n "AUDIO_CLEANUP" app/services/pipeline.py` — should show event logging
  Run: `python -c "from app.services.pipeline import PipelineOrchestrator; print('import ok')"` — should succeed
  </verify>
  <done>Pipeline runs audio retention cleanup as Step 10 after email delivery and run record update. Files older than AUDIO_RETENTION_DAYS (default 90) are deleted with per-file structlog and summary api_events logging. Cleanup failures never crash the pipeline.</done>
</task>

</tasks>

<verification>
1. `python -c "from app.models.api_event import ApiEventType; print(ApiEventType.AUDIO_CLEANUP)"` succeeds
2. `python -c "from app.services.pipeline import PipelineOrchestrator; print('ok')"` succeeds (no import errors)
3. grep confirms character_count appears in audio_generator.py _convert_to_audio detail dicts
4. grep confirms AUDIO_RETENTION_DAYS appears in pipeline.py
5. grep confirms Step 10 exists in pipeline.py run() method
</verification>

<success_criteria>
- TTS event detail JSON includes character_count and role for all future TTS_SUCCESS and TTS_FALLBACK events
- AUDIO_CLEANUP event type exists in ApiEventType enum
- Pipeline Step 10 runs cleanup with configurable AUDIO_RETENTION_DAYS (default 90)
- Empty date directories are removed after file cleanup
- Cleanup summary logged to both structlog and api_events
- Cleanup failure never crashes the pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/20-archive-operations/20-01-SUMMARY.md`
</output>
