---
phase: 17-audio-generation-foundation
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can run script generation for Brokers role and receive a 300-540 word podcast-style script with branded intro/outro"
    - "User can listen to generated MP3 audio and hear financial terms pronounced correctly"
    - "User can verify all four role audio files are 2-5 minutes duration and under 3 MB file size"
    - "User can confirm the same professional voice is used consistently across all four role briefings"
    - "User can observe that script content follows priority ordering matching the HTML brief structure"
  artifacts: []
  key_links: []
---

<objective>
Verify audio generation end-to-end by generating all four role briefings and validating quality.

Purpose: This is the acceptance verification plan. It generates real audio output from a completed pipeline run (or sample data), validates script content quality, pronunciation correctness, audio duration, file sizes, and voice consistency across all four roles. This plan gates phase completion.

Output: Verified audio briefings for all four roles confirming Phase 17 success criteria.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-audio-generation-foundation/17-CONTEXT.md
@.planning/phases/17-audio-generation-foundation/17-01-SUMMARY.md
@.planning/phases/17-audio-generation-foundation/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate all four role audio briefings and validate programmatically</name>
  <files></files>
  <action>
    1. Run `python scripts/generate_audio.py --role all` to generate audio for all four roles.

    2. If no completed pipeline run exists in the database (no articles to generate from), create a minimal test by:
       - Checking if any articles exist in the database with roles assigned
       - If none exist, the script should handle this gracefully

    3. After generation, validate programmatically:
       - Check that 4 MP3 files exist in `data/audio/YYYY-MM-DD/` (today's date)
       - Verify each file size is between 100KB and 5MB
       - Log file sizes and estimated durations (file_size_bytes / (128000/8) = seconds at 128kbps)
       - Verify the estimated duration is between 90 and 360 seconds (1.5 - 6 minutes, generous bounds)

    4. Run idempotent re-generation test:
       - Run `python scripts/generate_audio.py --role brokers` again
       - Verify it skips generation (logs "already_exists")
       - Verify file was NOT overwritten (same modification time)

    5. Run force regeneration test:
       - Run `python scripts/generate_audio.py --role brokers --force`
       - Verify it regenerates (logs "generating_audio")

    If Azure OpenAI is not configured or no database articles exist, log clearly what's missing and mark the task as blocked with instructions for the user.
  </action>
  <verify>
    4 MP3 files exist in data/audio/YYYY-MM-DD/ directory. Each file is 100KB-5MB. Idempotent check works (re-run skips existing files). Force flag overrides idempotent check.
  </verify>
  <done>
    All four role audio files generated, file sizes validated, idempotent behavior confirmed, force override works.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete audio briefing pipeline: Script generation (GPT-4o) -> Text preprocessing (num2words) -> TTS conversion (Azure OpenAI tts-1-hd) producing MP3 files for Brokers, Leadership, Compliance, and Underwriting roles.
  </what-built>
  <how-to-verify>
    1. Navigate to `data/audio/YYYY-MM-DD/` (today's date) and confirm 4 MP3 files exist
    2. Play `brokers.mp3` — listen for:
       - Branded intro: "Good morning, this is your Marsh Brokers intelligence brief for..."
       - Priority-ordered content (Critical stories mentioned first)
       - Source attribution ("Reuters reports...", "According to...")
       - Clean sign-off: "That's your Brokers brief for today. Stay informed."
       - Natural pronunciation of financial figures (no "$1.2M" read literally)
       - Professional female voice (nova)
    3. Play `leadership.mp3` — confirm SAME voice as brokers.mp3 (AUDIO-04 requirement)
    4. Play `compliance.mp3` and `underwriting.mp3` — confirm same voice, different content focus
    5. Check duration: each file should be approximately 2-5 minutes
    6. Note any pronunciation issues (company names, abbreviations, numbers) for future dictionary updates
  </how-to-verify>
  <resume-signal>Type "approved" if audio quality is acceptable, or describe specific issues (pronunciation, voice, content, duration)</resume-signal>
</task>

</tasks>

<verification>
1. Four MP3 files generated at data/audio/YYYY-MM-DD/{role}.mp3
2. Each file is 2-5 minutes duration and under 3 MB
3. Same voice (nova) used consistently across all four roles
4. Scripts contain branded intro and sign-off
5. Content follows priority ordering
6. Financial terms pronounced naturally
7. Idempotent generation confirmed
</verification>

<success_criteria>
- All five Phase 17 success criteria from ROADMAP.md are met:
  1. Brokers script is 300-540 words with branded intro/outro
  2. Financial terms pronounced correctly in MP3 audio
  3. All four role audio files are 2-5 minutes and under 3 MB
  4. Same professional voice across all four roles
  5. Script content follows priority ordering
- User has listened to at least one audio briefing and approved quality
</success_criteria>

<output>
After completion, create `.planning/phases/17-audio-generation-foundation/17-03-SUMMARY.md`
</output>
