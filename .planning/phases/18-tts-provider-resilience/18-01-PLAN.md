---
phase: 18-tts-provider-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/tts/__init__.py
  - app/services/tts/base.py
  - app/services/tts/azure_provider.py
  - app/services/tts/elevenlabs_provider.py
  - app/models/api_event.py
  - app/config.py
  - requirements.txt
autonomous: true
user_setup:
  - service: elevenlabs
    why: "Fallback TTS provider for audio generation"
    env_vars:
      - name: ELEVENLABS_API_KEY
        source: "ElevenLabs Dashboard -> Profile + API key"
      - name: ELEVENLABS_VOICE_ID
        source: "ElevenLabs Voice Library -> select professional female voice -> copy Voice ID"

must_haves:
  truths:
    - "TTSProvider abstract base class enforces synthesize() and provider_name interface"
    - "AzureTTSProvider produces MP3 using atomic file writes matching existing audio_generator.py pattern"
    - "ElevenLabsTTSProvider produces MP3 using atomic file writes with elevenlabs SDK"
    - "Both providers translate provider-specific exceptions to common TTSError"
    - "ApiEventType enum includes TTS_SUCCESS and TTS_FALLBACK values"
    - "Settings includes elevenlabs_api_key, elevenlabs_voice_id, and tts_voice fields"
  artifacts:
    - path: "app/services/tts/base.py"
      provides: "TTSProvider ABC and TTSError exception"
      contains: "class TTSProvider"
    - path: "app/services/tts/azure_provider.py"
      provides: "Azure OpenAI TTS implementation"
      contains: "class AzureTTSProvider"
    - path: "app/services/tts/elevenlabs_provider.py"
      provides: "ElevenLabs TTS implementation"
      contains: "class ElevenLabsTTSProvider"
    - path: "app/models/api_event.py"
      provides: "TTS event types in ApiEventType enum"
      contains: "TTS_SUCCESS"
    - path: "app/config.py"
      provides: "ElevenLabs configuration fields"
      contains: "elevenlabs_api_key"
  key_links:
    - from: "app/services/tts/azure_provider.py"
      to: "app/services/tts/base.py"
      via: "inherits TTSProvider, raises TTSError"
      pattern: "class AzureTTSProvider\\(TTSProvider\\)"
    - from: "app/services/tts/elevenlabs_provider.py"
      to: "app/services/tts/base.py"
      via: "inherits TTSProvider, raises TTSError"
      pattern: "class ElevenLabsTTSProvider\\(TTSProvider\\)"
---

<objective>
Create the TTS provider abstraction layer with Azure and ElevenLabs implementations, plus supporting config and enum changes.

Purpose: Establish the Strategy pattern foundation that Plan 02 will wire into AudioBriefingService for automatic failover. Both providers must implement the same interface (TTSProvider ABC) so they are interchangeable.

Output: `app/services/tts/` package with base class, two provider implementations, updated Settings with ElevenLabs config, updated ApiEventType with TTS event types, and elevenlabs SDK in requirements.txt.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-tts-provider-resilience/18-RESEARCH.md
@.planning/phases/17-audio-generation-foundation/17-02-SUMMARY.md
@app/services/audio_generator.py
@app/models/api_event.py
@app/config.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TTS provider package with base class and Azure provider</name>
  <files>
    app/services/tts/__init__.py
    app/services/tts/base.py
    app/services/tts/azure_provider.py
    app/models/api_event.py
    app/config.py
    requirements.txt
  </files>
  <action>
    Create the `app/services/tts/` package:

    **app/services/tts/__init__.py:**
    Standard package init. Import and re-export TTSProvider, TTSError, AzureTTSProvider, ElevenLabsTTSProvider for convenient access.

    **app/services/tts/base.py:**
    - Define `TTSError(Exception)` as common exception class for all TTS failures
    - Define `TTSProvider(ABC)` abstract base class with:
      - `synthesize(self, text: str, output_path: Path) -> dict` abstract method. Returns dict with keys: path (str), size_bytes (int), size_mb (float), provider (str), voice (str), model (str). Raises TTSError on failure.
      - `provider_name` abstract property returning str ("azure" or "elevenlabs")
    - Include docstrings matching the research patterns

    **app/services/tts/azure_provider.py:**
    - Define `AzureTTSProvider(TTSProvider)` that extracts TTS logic from current `audio_generator.py` `_convert_to_audio()`:
      - `__init__(self)`: Initialize OpenAI/AzureOpenAI client using same corporate proxy pattern from audio_generator.py lines 63-81. Read settings for endpoint, api_key, api_version. Store model ("tts-1-hd"), voice (from settings.tts_voice or "nova"), response_format ("mp3"), speed (1.0). Raise TTSError if Azure OpenAI not configured.
      - `synthesize(self, text: str, output_path: Path) -> dict`: Create parent dirs. Use atomic write (temp file + rename). Call `self.client.audio.speech.create()` and `response.stream_to_file()`. Return metadata dict. Wrap all openai exceptions (APIError, RateLimitError, APIConnectionError, APITimeoutError) in TTSError with chaining (`raise TTSError(...) from e`). Also catch generic Exception as TTSError.
      - `provider_name` property returns "azure"
    - Use structlog for logging (same patterns as audio_generator.py)
    - Do NOT add tenacity retry here -- retries stay in audio_generator.py (Plan 02 handles this)

    **app/models/api_event.py:**
    Add two new enum values to `ApiEventType`:
    ```python
    # TTS events (Phase 18):
    TTS_SUCCESS = "tts_success"      # TTS conversion succeeded (primary or fallback)
    TTS_FALLBACK = "tts_fallback"    # Fell back from primary to fallback TTS provider
    ```
    Add TTS section to the docstring following the existing pattern (Auth, News, Equity, Email, then TTS).

    **app/config.py:**
    Add three new fields to Settings class after the Azure OpenAI section:
    ```python
    # TTS Settings
    tts_voice: str = "nova"                # Azure OpenAI TTS voice name
    elevenlabs_api_key: str = ""           # ElevenLabs API key for fallback TTS
    elevenlabs_voice_id: str = ""          # ElevenLabs voice ID (find match for "nova")
    ```
    Add helper method `is_elevenlabs_configured(self) -> bool` that returns True if both elevenlabs_api_key and elevenlabs_voice_id are non-empty.

    **requirements.txt:**
    Add `elevenlabs` after the num2words line:
    ```
    # ElevenLabs TTS (fallback provider for audio generation)
    elevenlabs
    ```

    **Important constraints:**
    - Azure provider must preserve the exact atomic write pattern from audio_generator.py (temp file + rename + cleanup in finally block)
    - Never let provider-specific exceptions (openai.APIError, etc.) leak through -- always wrap in TTSError
    - Use `from __future__ import annotations` is NOT needed (project doesn't use it)
    - Follow existing import style: absolute imports from app.*
  </action>
  <verify>
    1. `python -c "from app.services.tts import TTSProvider, TTSError, AzureTTSProvider"` succeeds
    2. `python -c "from app.services.tts.base import TTSProvider; TTSProvider()" raises TypeError (cannot instantiate ABC)
    3. `python -c "from app.models.api_event import ApiEventType; print(ApiEventType.TTS_SUCCESS.value)"` prints "tts_success"
    4. `python -c "from app.config import get_settings; s = get_settings(); print(s.tts_voice, s.elevenlabs_api_key)"` prints "nova " (empty string)
    5. `python -c "from app.config import get_settings; print(get_settings().is_elevenlabs_configured())"` prints False
    6. Verify elevenlabs appears in requirements.txt
  </verify>
  <done>
    TTSProvider ABC enforces interface. AzureTTSProvider implements synthesize() with atomic writes and exception translation. ApiEventType has TTS_SUCCESS and TTS_FALLBACK. Settings has tts_voice, elevenlabs_api_key, elevenlabs_voice_id with is_elevenlabs_configured(). elevenlabs in requirements.txt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ElevenLabs TTS provider implementation</name>
  <files>
    app/services/tts/elevenlabs_provider.py
    app/services/tts/__init__.py
  </files>
  <action>
    **app/services/tts/elevenlabs_provider.py:**
    - Define `ElevenLabsTTSProvider(TTSProvider)`:
      - `__init__(self)`: Read settings for elevenlabs_api_key and elevenlabs_voice_id. Create ElevenLabs client: `from elevenlabs.client import ElevenLabs; self.client = ElevenLabs(api_key=settings.elevenlabs_api_key)`. Store voice_id and model_id ("eleven_multilingual_v2"). Raise TTSError if ElevenLabs not configured (using settings.is_elevenlabs_configured()).
      - `synthesize(self, text: str, output_path: Path) -> dict`: Create parent dirs. Use atomic write pattern (temp file + rename):
        ```python
        audio = self.client.text_to_speech.convert(
            text=text,
            voice_id=self.voice_id,
            model_id=self.model_id,
            output_format="mp3_44100_128"
        )
        ```
        The `audio` response from elevenlabs SDK is an iterator of bytes chunks. Write chunks to temp file: `for chunk in audio: temp_file.write(chunk)`. Then atomic rename. Return metadata dict with path, size_bytes, size_mb, provider="elevenlabs", voice=self.voice_id, model=self.model_id.
      - Wrap all ElevenLabs exceptions in TTSError. Import and catch broadly: `except Exception as e: raise TTSError(f"ElevenLabs TTS failed: {e}") from e`. The elevenlabs SDK may raise various exceptions (ApiError, etc.) -- catching Exception ensures nothing leaks.
      - `provider_name` property returns "elevenlabs"
    - Use structlog for logging (same patterns as azure_provider.py)
    - Must clean up temp file in finally block (same pattern as azure_provider.py)

    **app/services/tts/__init__.py:**
    Update to also import and re-export ElevenLabsTTSProvider:
    ```python
    from app.services.tts.base import TTSProvider, TTSError
    from app.services.tts.azure_provider import AzureTTSProvider
    from app.services.tts.elevenlabs_provider import ElevenLabsTTSProvider

    __all__ = ["TTSProvider", "TTSError", "AzureTTSProvider", "ElevenLabsTTSProvider"]
    ```

    **Important constraints:**
    - ElevenLabs SDK returns audio as an iterator (not bytes directly). Must iterate and write chunks.
    - Atomic write pattern MUST match Azure provider: temp_path = output_path.with_suffix('.tmp'), write to temp, rename to final, cleanup temp in finally block.
    - Do NOT use tenacity retry on ElevenLabs -- retries are handled at AudioBriefingService level (Plan 02).
    - File size validation happens at AudioBriefingService level, not in provider.
  </action>
  <verify>
    1. `python -c "from app.services.tts import ElevenLabsTTSProvider"` succeeds
    2. `python -c "from app.services.tts import TTSProvider, TTSError, AzureTTSProvider, ElevenLabsTTSProvider; print('all imports ok')"` succeeds
    3. `python -c "from app.services.tts.elevenlabs_provider import ElevenLabsTTSProvider; print(ElevenLabsTTSProvider.__bases__)"` shows TTSProvider as base class
    4. `pip install elevenlabs` succeeds (run pip install -r requirements.txt or just pip install elevenlabs)
  </verify>
  <done>
    ElevenLabsTTSProvider implements TTSProvider with atomic file writes and exception translation to TTSError. elevenlabs SDK installed. All four tts package exports (TTSProvider, TTSError, AzureTTSProvider, ElevenLabsTTSProvider) importable.
  </done>
</task>

</tasks>

<verification>
1. All TTS package imports work: `python -c "from app.services.tts import TTSProvider, TTSError, AzureTTSProvider, ElevenLabsTTSProvider"`
2. ABC enforcement: `python -c "from app.services.tts import TTSProvider; TTSProvider()"` raises TypeError
3. Both providers inherit from TTSProvider: check `issubclass(AzureTTSProvider, TTSProvider)` and `issubclass(ElevenLabsTTSProvider, TTSProvider)`
4. ApiEventType enum has TTS values: `python -c "from app.models.api_event import ApiEventType; print(ApiEventType.TTS_SUCCESS, ApiEventType.TTS_FALLBACK)"`
5. Settings has new fields: `python -c "from app.config import Settings; s = Settings(); print(s.tts_voice, s.is_elevenlabs_configured())"`
6. elevenlabs in requirements.txt
7. No provider-specific exceptions leak (both providers catch and wrap in TTSError)
</verification>

<success_criteria>
- TTSProvider ABC exists with synthesize() and provider_name abstract members
- AzureTTSProvider extracts TTS logic from audio_generator.py with atomic writes and TTSError wrapping
- ElevenLabsTTSProvider uses elevenlabs SDK with same atomic write and TTSError pattern
- ApiEventType has TTS_SUCCESS and TTS_FALLBACK enum values
- Settings has tts_voice, elevenlabs_api_key, elevenlabs_voice_id fields with is_elevenlabs_configured()
- elevenlabs package in requirements.txt and installable
- All imports resolve without errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-tts-provider-resilience/18-01-SUMMARY.md`
</output>
