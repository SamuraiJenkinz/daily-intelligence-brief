---
phase: 02-news-collection-scale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/sources/insurance_journal.py
  - app/services/sources/business_insurance.py
  - app/services/sources/artemis.py
  - app/services/sources/lloyds_list.py
  - app/services/sources/__init__.py
  - app/services/collector.py
autonomous: true

must_haves:
  truths:
    - "Collector can scrape Insurance Journal articles via Apify web-scraper actor"
    - "Collector can scrape Business Insurance articles via Apify web-scraper actor"
    - "Collector can scrape Artemis articles via Apify web-scraper actor"
    - "Collector can scrape Lloyd's List articles via Apify web-scraper actor"
    - "Each new source returns standardized article dicts matching existing schema"
    - "Failed source scrape returns empty list without halting pipeline"
  artifacts:
    - path: "app/services/sources/insurance_journal.py"
      provides: "InsuranceJournalSource scraper"
      contains: "class InsuranceJournalSource"
    - path: "app/services/sources/business_insurance.py"
      provides: "BusinessInsuranceSource scraper"
      contains: "class BusinessInsuranceSource"
    - path: "app/services/sources/artemis.py"
      provides: "ArtemisSource scraper"
      contains: "class ArtemisSource"
    - path: "app/services/sources/lloyds_list.py"
      provides: "LloydsListSource scraper"
      contains: "class LloydsListSource"
  key_links:
    - from: "app/services/collector.py"
      to: "app/services/sources/*.py"
      via: "source_map dict in _get_source_scraper()"
      pattern: "source_map.*Insurance Journal.*InsuranceJournalSource"
    - from: "app/services/sources/__init__.py"
      to: "all new source classes"
      via: "import and __all__ export"
      pattern: "from app.services.sources.insurance_journal import InsuranceJournalSource"
---

<objective>
Create Apify web-scraper actor implementations for the 4 priority news sources: Insurance Journal, Business Insurance, Artemis, and Lloyd's List.

Purpose: Expand collection from 1 source (Reinsurance News) to 5 sources, proving the polymorphic NewsSource pattern scales. These are the highest-value insurance industry news sites identified in the prototype.

Output: Four new source scraper files following the ReinsuranceNewsSource pattern, integrated into the collector's source_map.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\BrasilIntel\mdinsights\.planning\PROJECT.md
@C:\BrasilIntel\mdinsights\.planning\ROADMAP.md
@C:\BrasilIntel\mdinsights\.planning\STATE.md
@C:\BrasilIntel\mdinsights\.planning\phases\02-news-collection-scale\02-RESEARCH.md

Key existing files to reference:
@C:\BrasilIntel\mdinsights\app\services\sources\base.py
@C:\BrasilIntel\mdinsights\app\services\sources\reinsurance_news.py
@C:\BrasilIntel\mdinsights\app\services\collector.py
@C:\BrasilIntel\mdinsights\app\services\sources\__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 Apify source scrapers</name>
  <files>
    app/services/sources/insurance_journal.py
    app/services/sources/business_insurance.py
    app/services/sources/artemis.py
    app/services/sources/lloyds_list.py
  </files>
  <action>
Create four new source scraper files, each following the exact same pattern as ReinsuranceNewsSource in `app/services/sources/reinsurance_news.py`. Each class must:

1. Extend `NewsSource` ABC from `app/services/sources/base.py`
2. Implement `scrape()` returning `List[Dict[str, Any]]`
3. Use `self.apify_client.actor("apify/web-scraper").call(run_input=run_input)` pattern
4. Include site-specific CSS selectors with MULTIPLE FALLBACK selectors (critical for resilience per RESEARCH.md Pitfall 1)
5. Include `_normalize_article()` method matching existing pattern
6. Return empty list on failure (error handling strategy decision from Phase 1)
7. Use structlog for logging with `source=` binding

**Insurance Journal** (`insurance_journal.py`):
- Class: `InsuranceJournalSource`
- URL: https://www.insurancejournal.com/
- Source name: "Insurance Journal"
- CSS selectors (with fallbacks): `article, .article-item, .news-item` for containers; `h2 a, h3 a, .article-title a` for titles; `.article-excerpt, .summary, p` for descriptions
- linkSelector: `article a[href], .article-title a[href]`
- maxRequestsPerCrawl: 1 (homepage only)

**Business Insurance** (`business_insurance.py`):
- Class: `BusinessInsuranceSource`
- URL: https://www.businessinsurance.com/
- Source name: "Business Insurance"
- CSS selectors (with fallbacks): `article, .article-card, .story` for containers; `h2 a, h3 a, .headline a` for titles; `.teaser, .deck, .excerpt, p` for descriptions
- linkSelector: `article a[href], .headline a[href]`
- maxRequestsPerCrawl: 1

**Artemis** (`artemis.py`):
- Class: `ArtemisSource`
- URL: https://www.artemis.bm/
- Source name: "Artemis"
- CSS selectors (with fallbacks): `article, .post, .entry` for containers; `h2 a, h3 a, .entry-title a` for titles; `.entry-content, .excerpt, p` for descriptions
- linkSelector: `article a[href], h2 a[href]`
- maxRequestsPerCrawl: 1

**Lloyd's List** (`lloyds_list.py`):
- Class: `LloydsListSource`
- URL: https://www.lloydslist.com/ (may require playwright-scraper if JS-heavy per RESEARCH.md Pitfall 4)
- Source name: "Lloyd's List"
- CSS selectors (with fallbacks): `article, .article-card, .story-card` for containers; `h2 a, h3 a, .article-title a` for titles; `.article-summary, .teaser, p` for descriptions
- For Lloyd's List, use `apify/playwright-scraper` instead of `apify/web-scraper` since it is likely JS-rendered. The playwright-scraper run_input uses `requestHandler` instead of `pageFunction`:
  ```
  run_input = {
      "startUrls": [{"url": self.source_url}],
      "requestHandler": "..." (playwright page handler),
      "maxRequestsPerCrawl": 1,
      "proxyConfiguration": {"useApifyProxy": True}
  }
  ```
  If unsure about playwright-scraper API, use web-scraper for all 4 and note Lloyd's List may need a future switch.

Each file should limit extraction to first 20 articles (matching existing pattern) and use Apify proxy.
  </action>
  <verify>
All four files exist and import correctly:
```bash
python -c "from app.services.sources.insurance_journal import InsuranceJournalSource; print('OK')"
python -c "from app.services.sources.business_insurance import BusinessInsuranceSource; print('OK')"
python -c "from app.services.sources.artemis import ArtemisSource; print('OK')"
python -c "from app.services.sources.lloyds_list import LloydsListSource; print('OK')"
```
  </verify>
  <done>Four source scraper classes exist, each extending NewsSource ABC, with site-specific CSS selectors and fallbacks, returning standardized article dicts.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate sources into collector and module exports</name>
  <files>
    app/services/sources/__init__.py
    app/services/collector.py
  </files>
  <action>
1. Update `app/services/sources/__init__.py`:
   - Import all 4 new source classes
   - Add them to `__all__` list
   - Maintain alphabetical order for readability

2. Update `app/services/collector.py`:
   - Add imports for new source classes from `app.services.sources`
   - Expand `source_map` dict in `_get_source_scraper()` method to include:
     - "Insurance Journal": InsuranceJournalSource
     - "Business Insurance": BusinessInsuranceSource
     - "Artemis": ArtemisSource
     - "Lloyd's List": LloydsListSource
   - Keep "Reinsurance News": ReinsuranceNewsSource as first entry
   - Add a comment separating Phase 1 and Phase 2 sources

Do NOT change any other methods in collector.py. The existing `collect_from_sources()`, `_scrape_source()`, and `_store_articles()` methods handle new sources automatically via the source_map pattern.
  </action>
  <verify>
```bash
python -c "from app.services.sources import InsuranceJournalSource, BusinessInsuranceSource, ArtemisSource, LloydsListSource; print('All imports OK')"
python -c "from app.services.collector import ApifyCollector; print('Collector imports OK')"
```
  </verify>
  <done>All 4 new source classes are exported from sources module and registered in collector source_map. Collector will automatically scrape any Source DB record matching these names.</done>
</task>

</tasks>

<verification>
1. All 4 new source files exist under `app/services/sources/`
2. Each class extends `NewsSource` and implements `scrape()` method
3. Each class uses multiple fallback CSS selectors
4. Each class returns empty list on failure (no pipeline blocking)
5. `__init__.py` exports all new classes
6. `collector.py` source_map maps all 5 source names to their classes
7. `python -c "from app.services.collector import ApifyCollector"` succeeds without errors
</verification>

<success_criteria>
- 4 new Apify source scraper files created following ReinsuranceNewsSource pattern
- Collector source_map expanded from 1 to 5 sources
- All imports resolve cleanly
- Each scraper has fallback CSS selectors for resilience
- Error handling returns empty list (not raising exceptions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-news-collection-scale/02-01-SUMMARY.md`
</output>
