---
phase: 08-polish-and-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_branding.py
  - app/templates/role_brief.html
  - app/templates/email/role_email.html
autonomous: true

must_haves:
  truths:
    - "Browser template CSS variables match prototype exactly (--marsh-blue: #00263e, --marsh-light-blue: #0077c8, --marsh-accent: #00a3e0)"
    - "Email template uses Marsh brand colors in inline styles"
    - "Kevin Taylor / Colleague Technology Services attribution appears in both templates"
    - "Automated verification script confirms brand compliance with zero issues"
  artifacts:
    - path: "scripts/verify_branding.py"
      provides: "Automated brand verification across all templates"
      min_lines: 80
    - path: "app/templates/role_brief.html"
      provides: "Browser template with verified Marsh branding"
      contains: "--marsh-blue"
    - path: "app/templates/email/role_email.html"
      provides: "Email template with verified Marsh branding"
      contains: "Kevin Taylor"
  key_links:
    - from: "scripts/verify_branding.py"
      to: "app/templates/role_brief.html"
      via: "file read and regex matching"
      pattern: "role_brief\\.html"
    - from: "scripts/verify_branding.py"
      to: "app/templates/email/role_email.html"
      via: "file read and regex matching"
      pattern: "role_email\\.html"
---

<objective>
Verify and refine Marsh brand compliance across browser and email templates, creating an automated verification script that confirms brand guidelines are met.

Purpose: Ensures REPT-09 (brand match) and REPT-10 (Kevin Taylor attribution) are satisfied for production deployment. The prototype (RefChyt/prototype_daily_intelligence_brief.html) is the reference standard.

Output: Brand verification script (scripts/verify_branding.py) and any CSS corrections needed in templates.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\BrasilIntel\mdinsights\.planning\PROJECT.md
@C:\BrasilIntel\mdinsights\.planning\ROADMAP.md
@C:\BrasilIntel\mdinsights\.planning\STATE.md
@C:\BrasilIntel\mdinsights\.planning\phases\08-polish-and-launch\08-RESEARCH.md

Key reference files:
@C:\BrasilIntel\RefChyt\prototype_daily_intelligence_brief.html (brand reference - the gold standard)
@C:\BrasilIntel\mdinsights\app\templates\role_brief.html (browser template to verify)
@C:\BrasilIntel\mdinsights\app\templates\email\role_email.html (email template to verify)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brand verification script</name>
  <files>scripts/verify_branding.py</files>
  <action>
Create scripts/verify_branding.py that programmatically verifies brand compliance by comparing templates against the prototype reference. The script must check:

1. **CSS Color Variables** (browser template only - email uses inline styles):
   - --marsh-blue: #00263e
   - --marsh-light-blue: #0077c8
   - --marsh-accent: #00a3e0
   - --alert-red: #dc3545
   - --alert-orange: #fd7e14
   - --alert-yellow: #ffc107
   - --success-green: #28a745
   - --neutral-gray: #6c757d
   - --bg-light: #f5f7fa

2. **Email Template Inline Colors** (email template uses inline styles, not CSS variables):
   - Header background: #00263e
   - Accent gradient: #00a3e0
   - Background: #f5f7fa

3. **Attribution** (both templates):
   - "Kevin Taylor" text present
   - "Colleague Technology Services" text present

4. **Typography** (browser template):
   - font-family includes 'Segoe UI'

5. **Confidential Banner** (both templates):
   - "CONFIDENTIAL" text present

6. **Structural elements** (both templates):
   - Company name placeholder ({{ company_name }})
   - Report date placeholder ({{ report_date }})

The script should:
- Use pathlib for file resolution (relative to script location)
- Print clear PASS/FAIL per check with details
- Exit 0 if all checks pass, exit 1 if any fail
- Support --verbose flag for detailed output
- Follow the code example pattern from 08-RESEARCH.md but expand coverage

Use this structure:
```python
"""Verify MDInsights templates match Marsh brand guidelines."""
import re
import sys
from pathlib import Path

MARSH_COLORS = { ... }
REQUIRED_STRINGS = { ... }

def check_browser_template(path): ...
def check_email_template(path): ...
def main(): ...
```
  </action>
  <verify>Run `python scripts/verify_branding.py` from C:\BrasilIntel\mdinsights — expect exit code 0 with all checks passing.</verify>
  <done>Brand verification script exists, runs without errors, and confirms both templates match Marsh brand guidelines.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any brand discrepancies found by verification</name>
  <files>app/templates/role_brief.html, app/templates/email/role_email.html</files>
  <action>
After running the verification script from Task 1, fix any discrepancies found. Based on initial review, templates already closely match the prototype. Potential areas to verify and correct if needed:

1. **Browser template (role_brief.html):**
   - Confirm CSS variables at lines 8-18 match prototype exactly
   - Confirm header gradient matches: `linear-gradient(135deg, var(--marsh-blue) 0%, #003d6b 50%, var(--marsh-light-blue) 100%)`
   - Confirm font-family: `'Segoe UI', Tahoma, Geneva, Verdana, sans-serif`
   - Confirm confidential banner styling matches prototype
   - Confirm Kevin Taylor attribution pill styling matches prototype

2. **Email template (role_email.html):**
   - Confirm inline color #00263e used for header background
   - Confirm Kevin Taylor attribution present with gradient pill background
   - Confirm confidential banner text present
   - Confirm Segoe UI in font-family stack

If the verification script from Task 1 reports zero issues, this task confirms "no changes needed" and documents that fact.

IMPORTANT: Do NOT redesign or refactor templates. Only fix specific discrepancies against the prototype. This is verification, not a design project.

After any fixes, re-run `python scripts/verify_branding.py` to confirm all checks pass.
  </action>
  <verify>Run `python scripts/verify_branding.py` — must exit 0 with all checks passing. If changes were made, diff shows only targeted color/typography corrections, not structural changes.</verify>
  <done>Both templates verified as brand-compliant. Verification script passes with zero issues.</done>
</task>

</tasks>

<verification>
1. `python scripts/verify_branding.py` exits with code 0
2. Both templates contain correct Marsh color palette
3. Both templates contain Kevin Taylor / Colleague Technology Services attribution
4. No structural template changes made (only targeted corrections if needed)
</verification>

<success_criteria>
- Brand verification script exists at scripts/verify_branding.py and passes
- Browser template matches prototype color palette and typography
- Email template uses correct inline Marsh brand colors
- Kevin Taylor attribution confirmed in both templates
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish-and-launch/08-01-SUMMARY.md`
</output>
