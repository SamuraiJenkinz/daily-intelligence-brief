---
phase: 03-advanced-classification-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/migrate_003_classification_fields.py
  - app/models/news_article.py
  - app/schemas/classification.py
autonomous: true

must_haves:
  truths:
    - "NewsArticle ORM model has columns for entities, impact_level, category, region, business_line"
    - "Pydantic ArticleClassification schema includes ExtractedEntity model and all new typed fields"
    - "Database migration script adds 5 new nullable columns to news_articles table without breaking existing data"
    - "Existing articles with NULL new fields load without validation errors"
  artifacts:
    - path: "scripts/migrate_003_classification_fields.py"
      provides: "Database migration adding 5 new columns to news_articles"
      contains: "ALTER TABLE news_articles ADD COLUMN"
    - path: "app/models/news_article.py"
      provides: "ORM model with Phase 3 classification columns"
      contains: "impact_level"
    - path: "app/schemas/classification.py"
      provides: "Expanded Pydantic schema with entity extraction and categorical types"
      contains: "ExtractedEntity"
  key_links:
    - from: "app/schemas/classification.py"
      to: "app/models/news_article.py"
      via: "field names match between Pydantic schema and ORM columns"
      pattern: "impact_level|category|region|business_line|entities"
    - from: "scripts/migrate_003_classification_fields.py"
      to: "app/models/news_article.py"
      via: "migration column names match ORM column definitions"
      pattern: "ADD COLUMN (entities|impact_level|category|region|business_line)"
---

<objective>
Expand the data layer for Phase 3 advanced classification: add new columns to the database, extend the ORM model, and expand the Pydantic schema with entity extraction and categorical types.

Purpose: The existing classifier only stores roles, priority, summary, and sentiment. Phase 3 requires storing entities (companies/people/organizations), impact level, category, region, and business line per article. This plan creates the foundation that the classifier update (Plan 02) will write into.

Output: Migration script, updated ORM model, expanded Pydantic schema with ExtractedEntity and Literal types for all new fields.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\BrasilIntel\mdinsights\.planning\PROJECT.md
@C:\BrasilIntel\mdinsights\.planning\ROADMAP.md
@C:\BrasilIntel\mdinsights\.planning\STATE.md
@C:\BrasilIntel\mdinsights\.planning\phases\03-advanced-classification-pipeline\03-RESEARCH.md
@C:\BrasilIntel\mdinsights\app\models\news_article.py
@C:\BrasilIntel\mdinsights\app\schemas\classification.py
@C:\BrasilIntel\mdinsights\app\database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration script and ORM model expansion</name>
  <files>
    scripts/migrate_003_classification_fields.py
    app/models/news_article.py
  </files>
  <action>
Create a migration script at `scripts/migrate_003_classification_fields.py` that adds 5 new nullable columns to the `news_articles` table using SQLite ADD COLUMN statements (NOT Alembic -- the project does not use Alembic and should not introduce it for simple column additions).

The migration script should:
1. Import from `app.database` (SessionLocal, engine)
2. Use `engine.execute()` or `sqlalchemy.text()` with raw SQL for each column:
   - `ALTER TABLE news_articles ADD COLUMN entities TEXT` (JSON array of extracted entities)
   - `ALTER TABLE news_articles ADD COLUMN impact_level VARCHAR(20)` (Critical/High/Medium/Low)
   - `ALTER TABLE news_articles ADD COLUMN category VARCHAR(50)` (M&A/Regulatory/Loss Event/etc.)
   - `ALTER TABLE news_articles ADD COLUMN region VARCHAR(50)` (North America/Europe/etc.)
   - `ALTER TABLE news_articles ADD COLUMN business_line VARCHAR(50)` (Property/Casualty/etc.)
3. Wrap each ALTER TABLE in try/except to handle "duplicate column name" gracefully (idempotent migration)
4. Print status for each column added
5. Follow the pattern of existing scripts: `sys.path.insert(0, str(project_root))` at top, `if __name__ == "__main__":` block

Then update `app/models/news_article.py` to add the 5 new columns to the NewsArticle ORM class:
- `entities = Column(Text, nullable=True)` -- JSON array of entity objects, add comment: `# JSON array of {name, type, context} objects`
- `impact_level = Column(String(20), nullable=True)` -- Critical, High, Medium, Low
- `category = Column(String(50), nullable=True)` -- M&A, Regulatory, Loss Event, etc.
- `region = Column(String(50), nullable=True)` -- North America, Europe, etc.
- `business_line = Column(String(50), nullable=True)` -- Property, Casualty, etc.

Place the new columns AFTER the existing `sentiment` column and BEFORE `created_at`. Update the docstring to mention Phase 3 fields.

IMPORTANT: All new columns must be nullable=True for backward compatibility with existing Phase 1/2 articles that have these fields as NULL.
  </action>
  <verify>
Run `python scripts/migrate_003_classification_fields.py` from the mdinsights directory -- it should print success messages for each column added (or "already exists" on re-run). Then verify with: `python -c "from app.models.news_article import NewsArticle; print([c.name for c in NewsArticle.__table__.columns])"` -- should show all original + 5 new column names.
  </verify>
  <done>
Migration script runs idempotently (safe to run multiple times). NewsArticle ORM model has entities, impact_level, category, region, business_line columns. Existing articles with NULL values for new fields load without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand Pydantic classification schema with entity extraction and categorical types</name>
  <files>
    app/schemas/classification.py
  </files>
  <action>
Expand `app/schemas/classification.py` to add Phase 3 fields while preserving all existing types and the existing ArticleClassification field structure.

Add BEFORE the ArticleClassification class:

1. `ExtractedEntity` Pydantic model:
   - `name: str` with Field description "Entity name normalized to official form (e.g., 'Marsh McLennan' not 'Marsh')"
   - `type: Literal["company", "person", "organization"]` with Field description explaining each type (company = insurers/brokers/vendors, person = executives/analysts, organization = regulators/associations)
   - `context: str` with Field description "Brief context of entity's role in the article (1 sentence)"

2. Add new Literal type aliases (alongside existing RoleType, PriorityType, SentimentType):
   - `ImpactLevelType = Literal["Critical", "High", "Medium", "Low"]`
   - `CategoryType = Literal["M&A", "Regulatory", "Loss Event", "Financial Results", "Market Trends", "Product Launch", "Executive Change", "Other"]`
   - `RegionType = Literal["North America", "Europe", "Asia Pacific", "Latin America", "Middle East & Africa", "Global"]`
   - `BusinessLineType = Literal["Property", "Casualty", "Life & Health", "Reinsurance", "Specialty", "Multiple Lines", "Other"]`

3. Add to ArticleClassification class (AFTER existing sentiment field):
   - `entities: List[ExtractedEntity] = Field(default_factory=list, description="3-10 most relevant companies, people, and organizations mentioned in article")`
   - `impact_level: ImpactLevelType = Field(description="Market impact: Critical ($1B+/systemic), High ($100M-$1B), Medium ($10M-$100M), Low (<$10M/routine)")`
   - `category: CategoryType = Field(description="Primary article category based on content theme")`
   - `region: RegionType = Field(description="Primary geographic region. Use Global for multi-region stories.")`
   - `business_line: BusinessLineType = Field(description="Primary insurance business line affected. Use Multiple Lines for cross-segment.")`

4. Update the `json_schema_extra` example to include new fields:
   ```python
   "entities": [
       {"name": "Marsh McLennan", "type": "company", "context": "Acquiring broker expanding into specialty lines"},
       {"name": "John Smith", "type": "person", "context": "CEO of Marsh McLennan announcing acquisition"}
   ],
   "impact_level": "High",
   "category": "M&A",
   "region": "North America",
   "business_line": "Specialty"
   ```

5. Update imports to include `Optional` from typing (for future use) and ensure `Literal` is imported.

IMPORTANT: Do NOT change any existing field definitions (roles, priority, summary, sentiment). Only ADD new fields and types. The structured outputs API will enforce all Literal constraints automatically.
  </action>
  <verify>
Run `python -c "from app.schemas.classification import ArticleClassification, ExtractedEntity; print(ArticleClassification.model_json_schema())"` -- should output JSON schema with all 9 fields (4 existing + 5 new) and ExtractedEntity nested definition. Verify no import errors.
  </verify>
  <done>
ArticleClassification schema has entities (List[ExtractedEntity]), impact_level, category, region, business_line fields with proper Literal type constraints. ExtractedEntity model has name, type, context fields. All existing fields unchanged. Schema generates valid JSON schema for structured outputs API.
  </done>
</task>

</tasks>

<verification>
1. `python scripts/migrate_003_classification_fields.py` runs without errors
2. `python -c "from app.models.news_article import NewsArticle; cols = [c.name for c in NewsArticle.__table__.columns]; assert 'entities' in cols and 'impact_level' in cols and 'category' in cols and 'region' in cols and 'business_line' in cols; print('ORM OK:', cols)"` passes
3. `python -c "from app.schemas.classification import ArticleClassification, ExtractedEntity; schema = ArticleClassification.model_json_schema(); assert 'entities' in schema['properties']; assert 'impact_level' in schema['properties']; print('Schema OK')"` passes
4. `python -c "from app.schemas.classification import ArticleClassification; a = ArticleClassification(roles=['Brokers'], priority='High', summary='test', sentiment='neutral', entities=[], impact_level='Medium', category='Other', region='Global', business_line='Other'); print('Validation OK:', a.model_dump())"` passes
</verification>

<success_criteria>
- Migration script adds 5 columns to database idempotently
- ORM model reflects all 5 new columns as nullable
- Pydantic schema has ExtractedEntity model and all categorical Literal types
- Existing code (classifier, pipeline) still imports and runs without errors
- Schema validates both old-style (Phase 1 fields only with defaults for new) and new-style (all fields) data
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-classification-pipeline/03-01-SUMMARY.md`
</output>
