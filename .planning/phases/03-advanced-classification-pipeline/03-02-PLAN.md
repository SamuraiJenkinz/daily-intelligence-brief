---
phase: 03-advanced-classification-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/services/classifier.py
autonomous: true

must_haves:
  truths:
    - "Classified articles have entity mentions with company/person/organization types and contextual descriptions"
    - "Each article receives an impact level (market magnitude) independent of its priority (Marsh urgency)"
    - "Articles are categorized by theme (M&A, Regulatory, etc.), region, and business line for downstream filtering"
    - "Single API call per article handles all 9 classification dimensions without multi-pass"
  artifacts:
    - path: "app/services/classifier.py"
      provides: "Expanded classifier with Phase 3 prompt and field storage"
      contains: "article.entities"
  key_links:
    - from: "app/services/classifier.py"
      to: "app/schemas/classification.py"
      via: "classify_article returns ArticleClassification with new fields"
      pattern: "classification\\.entities|classification\\.impact_level|classification\\.category"
    - from: "app/services/classifier.py"
      to: "app/models/news_article.py"
      via: "classify_articles writes new fields to NewsArticle ORM"
      pattern: "article\\.(entities|impact_level|category|region|business_line)"
---

<objective>
Expand the classifier service prompt and field storage to populate all Phase 3 classification fields in a single GPT-4o call.

Purpose: The expanded Pydantic schema (from Plan 01) defines the new fields, but the classifier prompt needs guidance for entity extraction, impact assessment, and categorical tagging. The classify_articles method needs to store the new fields into the database.

Output: Updated classifier.py with comprehensive prompt and field storage for all 9 classification dimensions.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\BrasilIntel\mdinsights\.planning\PROJECT.md
@C:\BrasilIntel\mdinsights\.planning\ROADMAP.md
@C:\BrasilIntel\mdinsights\.planning\phases\03-advanced-classification-pipeline\03-RESEARCH.md
@C:\BrasilIntel\mdinsights\.planning\phases\03-advanced-classification-pipeline\03-01-SUMMARY.md
@C:\BrasilIntel\mdinsights\app\services\classifier.py
@C:\BrasilIntel\mdinsights\app\schemas\classification.py
@C:\BrasilIntel\mdinsights\app\models\news_article.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CLASSIFICATION_PROMPT with entity extraction and categorical guidance</name>
  <files>app/services/classifier.py</files>
  <action>
Expand the `CLASSIFICATION_PROMPT` string in `app/services/classifier.py` by APPENDING new sections after the existing Sentiment Guidance section. Do NOT modify the existing role definitions, multi-role examples, priority guidance, or sentiment guidance sections -- they are working well.

APPEND these sections to the prompt:

**Entity Extraction Guidelines:**

```
**Entity Extraction Guidelines:**

Extract 3-10 of the MOST RELEVANT entities (companies, people, organizations) mentioned in the article:
- **Companies**: Insurance carriers, brokers, reinsurers, vendors (e.g., "Marsh McLennan", "AIG", "Lloyd's of London"). Normalize to official full names.
- **People**: Named executives, analysts, regulators with their title/role if mentioned (e.g., "John Smith" with context "CEO of Zurich Insurance").
- **Organizations**: Regulators, trade associations, rating agencies (e.g., "NAIC", "AM Best", "European Commission").

Prioritize entities with significant context or market impact. Skip generic mentions without meaningful context.
```

**Impact Level Guidelines:**

```
**Impact Level Assessment:**

Assess market impact independently from priority (priority = urgency for Marsh, impact = market magnitude):
- **Critical**: Market-moving events ($1B+ losses, major regulatory overhauls, systemic risk indicators)
- **High**: Significant market impact ($100M-$1B range, important M&A, notable regulatory shifts)
- **Medium**: Notable but contained ($10M-$100M, smaller M&A, local regulations, regional events)
- **Low**: Minor industry news (<$10M impact, routine updates, individual appointments, editorial commentary)
```

**Category Assignment:**

```
**Category Assignment:**

Assign the PRIMARY category that best describes the article's main theme:
- **M&A**: Mergers, acquisitions, divestitures, strategic partnerships, joint ventures
- **Regulatory**: New regulations, compliance requirements, legal rulings, policy reforms
- **Loss Event**: Natural catastrophes, claims developments, reserve adjustments, insured losses
- **Financial Results**: Earnings reports, rate changes, combined ratios, premium growth
- **Market Trends**: Industry analysis, forecasts, emerging risks, market outlook
- **Product Launch**: New products, coverage innovations, technology deployments
- **Executive Change**: C-suite appointments, leadership transitions, board changes
- **Other**: Articles not fitting above categories
```

**Region Assignment:**

```
**Region Assignment:**

Assign the PRIMARY geographic focus of the article:
- **North America**: US, Canada, Bermuda
- **Europe**: UK, EU, Switzerland, Nordic
- **Asia Pacific**: Australia, Japan, China, India, Southeast Asia
- **Latin America**: Central and South America, Caribbean
- **Middle East & Africa**: MENA region, Sub-Saharan Africa
- **Global**: Multi-region stories, worldwide market impacts, international organizations
```

**Business Line Assignment:**

```
**Business Line Assignment:**

Assign the PRIMARY insurance business line affected:
- **Property**: Property insurance, homeowners, commercial property, fire
- **Casualty**: Auto, general liability, workers compensation, professional liability
- **Life & Health**: Life insurance, health insurance, accident, disability
- **Reinsurance**: Reinsurance treaties, retrocession, ILS, catastrophe bonds
- **Specialty**: Cyber, D&O, marine, aviation, political risk, niche coverages
- **Multiple Lines**: Cross-segment stories affecting 3+ business lines, multi-line carriers
- **Other**: Not clearly aligned with a specific business line
```
  </action>
  <verify>
Run `python -c "from app.services.classifier import CLASSIFICATION_PROMPT; assert 'Entity Extraction' in CLASSIFICATION_PROMPT; assert 'Impact Level' in CLASSIFICATION_PROMPT; assert 'Category Assignment' in CLASSIFICATION_PROMPT; assert 'Region Assignment' in CLASSIFICATION_PROMPT; assert 'Business Line' in CLASSIFICATION_PROMPT; print('Prompt sections OK, length:', len(CLASSIFICATION_PROMPT))"` -- should print OK with prompt length around 3000-4000 chars.
  </verify>
  <done>CLASSIFICATION_PROMPT includes all 5 new guidance sections appended after existing content. Existing role/priority/sentiment guidance unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update classify_articles to store new Phase 3 fields</name>
  <files>app/services/classifier.py</files>
  <action>
Update the `classify_articles` method in `RoleClassificationService` to store the 5 new Phase 3 fields when writing classification results to articles.

In the section where article fields are updated (after `classification = self.classify_article(...)` call), ADD these lines AFTER the existing `article.sentiment = classification.sentiment` line:

```python
# Phase 3: Advanced classification fields
article.entities = json.dumps([e.model_dump() for e in classification.entities])
article.impact_level = classification.impact_level
article.category = classification.category
article.region = classification.region
article.business_line = classification.business_line
```

Note: Use `e.model_dump()` (Pydantic v2 method) NOT `e.dict()` (deprecated Pydantic v1 method).

Also update the logging in `classify_article` method to include new fields. After the existing log line that logs roles/priority/sentiment, add impact_level and category to the log kwargs:

```python
self.logger.info(
    "article_classified",
    title=title[:50],
    roles=classification.roles,
    priority=classification.priority,
    sentiment=classification.sentiment,
    impact_level=classification.impact_level,
    category=classification.category,
    entity_count=len(classification.entities)
)
```

Do NOT change:
- The `classify_article` method signature or API call logic (it already uses ArticleClassification as response_format, which now includes new fields automatically)
- The skip-on-error pattern in `classify_articles`
- The batch commit every 10 articles logic
- The already-classified check (`if article.roles is not None`)
  </action>
  <verify>
Run `python -c "from app.services.classifier import RoleClassificationService; import inspect; src = inspect.getsource(RoleClassificationService.classify_articles); assert 'article.entities' in src; assert 'article.impact_level' in src; assert 'article.category' in src; assert 'article.region' in src; assert 'article.business_line' in src; assert 'model_dump' in src; print('Field storage OK')"` -- verifies all 5 new fields are stored and model_dump is used for entities serialization.
  </verify>
  <done>classify_articles stores all 5 new fields (entities as JSON, impact_level, category, region, business_line) into NewsArticle records. Logging includes new dimensions. No changes to API call, error handling, or batch patterns.</done>
</task>

</tasks>

<verification>
1. `python -c "from app.services.classifier import CLASSIFICATION_PROMPT, RoleClassificationService; print('Import OK')"` -- no import errors
2. Prompt contains all 5 new guidance sections (entity, impact, category, region, business line)
3. `classify_articles` method writes entities (as JSON via model_dump), impact_level, category, region, business_line to each article
4. Existing classification flow unchanged: same API call, same error handling, same batch commits
5. No breaking changes to pipeline.py (it calls classifier.classify_articles with same signature)
</verification>

<success_criteria>
- CLASSIFICATION_PROMPT has comprehensive guidance for all 9 classification dimensions
- classify_articles stores all Phase 3 fields into database
- Single GPT-4o API call handles all fields (no multi-pass)
- Existing error handling and batch patterns preserved
- Pipeline orchestrator requires zero changes
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-classification-pipeline/03-02-SUMMARY.md`
</output>
