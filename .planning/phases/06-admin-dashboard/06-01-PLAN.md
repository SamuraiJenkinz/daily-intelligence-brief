---
phase: 06-admin-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/admin/base.html
  - app/templates/admin/dashboard.html
  - app/routers/admin.py
  - app/main.py
autonomous: true

must_haves:
  truths:
    - "Navigating to /admin shows a Bootstrap 5 dashboard with sidebar navigation"
    - "Sidebar links exist for Sources, Recipients, Archive, Search, and Manual Trigger"
    - "Dashboard landing page shows system status summary (source count, recent runs, article count)"
    - "All admin pages inherit from base.html with HTMX and Bootstrap loaded via CDN"
  artifacts:
    - path: "app/templates/admin/base.html"
      provides: "Bootstrap 5 + HTMX base layout with sidebar navigation"
      contains: "htmx.org@2.0.4"
    - path: "app/templates/admin/dashboard.html"
      provides: "Dashboard landing page with system stats"
      contains: "extends"
    - path: "app/routers/admin.py"
      provides: "Admin routes including GET /admin dashboard"
      exports: ["router"]
  key_links:
    - from: "app/templates/admin/base.html"
      to: "CDN scripts"
      via: "script/link tags"
      pattern: "bootstrap@5\\.3\\.3|htmx\\.org@2\\.0\\.4"
    - from: "app/routers/admin.py"
      to: "app/templates/admin/"
      via: "Jinja2 template rendering"
      pattern: "jinja_env\\.get_template.*admin/"
    - from: "app/main.py"
      to: "app/routers/admin.py"
      via: "include_router"
      pattern: "include_router.*admin_router"
---

<objective>
Create the Bootstrap 5 + HTMX base template and admin dashboard landing page that all other Phase 6 plans build upon.

Purpose: Establish the visual foundation and navigation structure for the entire admin dashboard. Every subsequent plan (source management, archive, search, recipients, trigger) renders within this layout.
Output: base.html template with sidebar nav, dashboard.html landing page with system stats, refactored admin router with Jinja2 template directory pointing to admin/ subfolder.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/BrasilIntel/mdinsights/.planning/PROJECT.md
@C:/BrasilIntel/mdinsights/.planning/ROADMAP.md
@C:/BrasilIntel/mdinsights/.planning/STATE.md
@C:/BrasilIntel/mdinsights/.planning/phases/06-admin-dashboard/06-RESEARCH.md

@C:/BrasilIntel/mdinsights/app/routers/admin.py
@C:/BrasilIntel/mdinsights/app/main.py
@C:/BrasilIntel/mdinsights/app/database.py
@C:/BrasilIntel/mdinsights/app/models/source.py
@C:/BrasilIntel/mdinsights/app/models/run.py
@C:/BrasilIntel/mdinsights/app/templates/admin_trigger.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base.html template with Bootstrap 5 + HTMX + sidebar nav</name>
  <files>app/templates/admin/base.html</files>
  <action>
Create `app/templates/admin/base.html` as the master layout template for all admin pages.

Requirements:
- Load Bootstrap 5.3.3 CSS and JS from CDN (jsdelivr)
- Load Bootstrap Icons 1.11.3 from CDN (jsdelivr)
- Load HTMX 2.0.4 from CDN (unpkg)
- Fixed top navbar: dark background (--marsh-blue: #00263e), brand text "MDInsights Admin" with bi-speedometer2 icon, right-aligned link to /docs (API Docs)
- Fixed left sidebar (250px width, white bg, border-right):
  - Sources link (/admin/sources) with bi-globe icon
  - Recipients link (/admin/recipients) with bi-people icon
  - Archive link (/admin/archive) with bi-archive icon
  - Search link (/admin/search) with bi-search icon
  - Manual Trigger link (/admin/trigger) with bi-play-circle icon
  - Add "active" class highlighting based on `{% block active_nav %}` variable
- Main content area with proper margin-left for sidebar and margin-top for navbar
- Jinja2 blocks: {% block title %}, {% block active_nav %}, {% block content %}
- CSS custom properties for Marsh branding: --marsh-blue: #00263e, --marsh-light-blue: #0077c8
- Responsive: On screens < 768px, sidebar collapses (hidden by default, toggled by hamburger button in navbar)
- Add a {% block extra_head %} for page-specific CSS
- Add a {% block extra_scripts %} before closing body for page-specific JS

Do NOT use any local CSS/JS files -- all from CDN only.
  </action>
  <verify>
Confirm file exists at `app/templates/admin/base.html`. Verify it contains `bootstrap@5.3.3`, `htmx.org@2.0.4`, `bootstrap-icons@1.11.3` CDN links. Verify it has {% block title %}, {% block content %}, {% block active_nav %} blocks.
  </verify>
  <done>base.html exists with Bootstrap 5, HTMX, Bootstrap Icons CDN links, sidebar navigation with 5 links, responsive layout, and Marsh branding colors.</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard landing page and refactor admin router</name>
  <files>app/templates/admin/dashboard.html, app/routers/admin.py</files>
  <action>
1. Create `app/templates/admin/dashboard.html` extending base.html:
   - Set title block to "Dashboard"
   - Set active_nav block to "dashboard" (no sidebar item highlighted -- or add Dashboard as first sidebar item with bi-speedometer2)
   - Show summary cards in a Bootstrap grid (row with col-md-3 cards):
     - Card 1: "Active Sources" -- count of enabled sources (green badge)
     - Card 2: "Total Sources" -- count of all sources
     - Card 3: "Articles Today" -- count of articles from today's date
     - Card 4: "Last Run" -- status and timestamp of most recent run
   - Below cards: "Recent Runs" table showing last 10 runs with columns: ID, Status (badge: success=green, failed=red, running=blue), Started, Completed, Articles Collected, Articles Classified, Error
   - Table uses Bootstrap table-striped table-hover classes

2. Refactor `app/routers/admin.py`:
   - Change the Jinja2 FileSystemLoader to point to `templates` directory (keep existing path)
   - Add a new route: GET /admin -> returns dashboard.html (the landing page)
   - This route queries: Source count (total + enabled), NewsArticle count for today, last 10 Runs
   - Keep ALL existing routes (/admin/trigger, /admin/trigger-pipeline, /admin/runs) intact and unchanged
   - Import NewsArticle model and Source model for queries
   - Use the existing SessionLocal() pattern for DB access
   - For HTMX detection: check request.headers.get("HX-Request") -- if HTMX request, return partial content; if full page request, return full page with base.html layout

IMPORTANT: Do NOT break the existing /admin/trigger endpoint. It must continue to serve admin_trigger.html as before. The trigger page will be integrated into the dashboard layout in Plan 03.

IMPORTANT: Update root redirect from /docs to /admin so the admin dashboard is the default landing page.
  </action>
  <verify>
Start the server with `cd C:/BrasilIntel/mdinsights && python -m uvicorn app.main:app --port 8001` and verify:
1. GET http://localhost:8001/admin returns dashboard HTML with Bootstrap styling
2. GET http://localhost:8001/admin/trigger still works (serves trigger UI)
3. GET http://localhost:8001/admin/runs still returns JSON
4. Dashboard shows source counts and recent runs table
  </verify>
  <done>Dashboard landing page renders at /admin with system stats (source counts, article count, recent runs). All existing admin endpoints continue to work unchanged. Root / redirects to /admin.</done>
</task>

</tasks>

<verification>
1. `python -c "from app.routers.admin import router; print('Router imports OK')"` succeeds
2. Server starts without errors on port 8001
3. GET /admin returns HTML with Bootstrap 5 styling and sidebar navigation
4. GET /admin/trigger returns existing trigger UI
5. GET /admin/runs returns JSON array of runs
6. Sidebar contains links to /admin/sources, /admin/recipients, /admin/archive, /admin/search, /admin/trigger
</verification>

<success_criteria>
- base.html template exists with Bootstrap 5 + HTMX + sidebar nav
- Dashboard page shows system status summary
- All existing admin routes preserved
- Root redirects to /admin
- Server starts and serves pages without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-dashboard/06-01-SUMMARY.md`
</output>
