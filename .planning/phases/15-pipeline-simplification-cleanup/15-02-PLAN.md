---
phase: 15-pipeline-simplification-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/services/collector.py
  - app/services/sources/__init__.py
  - app/services/sources/artemis.py
  - app/services/sources/business_insurance.py
  - app/services/sources/insurance_journal.py
  - app/services/sources/lloyds_list.py
  - app/services/sources/reinsurance_news.py
  - app/services/sources/rss_source.py
  - scripts/test_collection.py
  - scripts/test_pipeline.py
  - scripts/seed_sources.py
  - scripts/validate_production_ready.py
autonomous: true

must_haves:
  truths:
    - "ApifyCollector class file (app/services/collector.py) is deleted from codebase"
    - "All Apify source implementation files (artemis, business_insurance, insurance_journal, lloyds_list, reinsurance_news, rss_source) are deleted"
    - "app/services/sources/__init__.py only exports NewsSource from base.py"
    - "base.py abstract interface is retained for future extensibility"
    - "No Python file in the project fails to import due to missing ApifyCollector or source files"
    - "Test and script files are updated to remove Apify references without breaking non-Apify functionality"
  artifacts:
    - path: "app/services/sources/base.py"
      provides: "Abstract NewsSource interface retained for future use"
      contains: "class NewsSource"
    - path: "app/services/sources/__init__.py"
      provides: "Minimal sources module exporting only NewsSource"
  key_links:
    - from: "app/services/sources/__init__.py"
      to: "app/services/sources/base.py"
      via: "import"
      pattern: "from app.services.sources.base import NewsSource"
---

<objective>
Delete all Apify collection infrastructure: the ApifyCollector class, all source implementation files, and update related scripts. Retain base.py for future extensibility per CONTEXT.md decision.

Purpose: CLEAN-01 and CLEAN-05 — remove dead code files that are no longer imported by the pipeline (Plan 01 already removed all references).

Output: ApifyCollector and 6 source files deleted. Sources module exports only NewsSource. Scripts updated.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-pipeline-simplification-cleanup/15-CONTEXT.md
@.planning/phases/15-pipeline-simplification-cleanup/15-RESEARCH.md
@.planning/phases/15-pipeline-simplification-cleanup/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ApifyCollector and Apify source implementation files</name>
  <files>
    app/services/collector.py
    app/services/sources/__init__.py
    app/services/sources/artemis.py
    app/services/sources/business_insurance.py
    app/services/sources/insurance_journal.py
    app/services/sources/lloyds_list.py
    app/services/sources/reinsurance_news.py
    app/services/sources/rss_source.py
  </files>
  <action>
  1. **Delete ApifyCollector file:**
     ```bash
     rm app/services/collector.py
     ```

  2. **Delete all Apify source implementation files** (keep base.py):
     ```bash
     rm app/services/sources/artemis.py
     rm app/services/sources/business_insurance.py
     rm app/services/sources/insurance_journal.py
     rm app/services/sources/lloyds_list.py
     rm app/services/sources/reinsurance_news.py
     rm app/services/sources/rss_source.py
     ```

  3. **Update app/services/sources/__init__.py** to only export base class:
     ```python
     """
     News source interface module.

     Contains abstract base class for news source scrapers.
     Concrete Apify/RSS implementations removed in Phase 15 (v1.2)
     when pipeline switched to Factiva-only collection.
     """
     from app.services.sources.base import NewsSource

     __all__ = ["NewsSource"]
     ```

  4. **Keep app/services/sources/base.py unchanged** — the abstract NewsSource interface is retained for future extensibility per Phase 15 CONTEXT.md decision.

  5. **Delete any __pycache__ directories** in the sources folder to avoid stale bytecode:
     ```bash
     rm -rf app/services/sources/__pycache__
     rm -rf app/services/__pycache__
     ```
  </action>
  <verify>
  Verify deleted files no longer exist:
  - `test -f app/services/collector.py && echo "FAIL" || echo "OK"` — must print OK
  - `test -f app/services/sources/artemis.py && echo "FAIL" || echo "OK"` — must print OK
  - `test -f app/services/sources/rss_source.py && echo "FAIL" || echo "OK"` — must print OK

  Verify retained files exist:
  - `test -f app/services/sources/base.py && echo "OK" || echo "FAIL"` — must print OK
  - `test -f app/services/sources/__init__.py && echo "OK" || echo "FAIL"` — must print OK

  Verify imports work:
  - `python -c "from app.services.sources import NewsSource; print('OK')"` — must succeed
  - `python -c "from app.services.sources.base import NewsSource; print('OK')"` — must succeed
  </verify>
  <done>
  ApifyCollector class deleted. Six Apify/RSS source implementation files deleted. Sources module exports only NewsSource abstract base. No dead code files remain in app/services/sources/ directory (only base.py and __init__.py).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update scripts to remove Apify references</name>
  <files>
    scripts/test_collection.py
    scripts/test_pipeline.py
    scripts/seed_sources.py
    scripts/validate_production_ready.py
  </files>
  <action>
  1. **scripts/test_collection.py** — This script explicitly tests ApifyCollector. Since ApifyCollector no longer exists, this script should be rewritten to test FactivaCollector instead:
     - Replace the import: `from app.services.collector import ApifyCollector` -> `from app.collectors.factiva import FactivaCollector`
     - Replace `is_apify_configured()` check with `FactivaCollector().is_configured()`
     - Replace `ApifyCollector(settings.apify_token)` with `FactivaCollector()`
     - Update the collection call to use FactivaCollector.collect() with query_params from FactivaConfig
     - Update the script description comments to reference Factiva instead of Apify
     - Update error messages: "APIFY_TOKEN not configured" -> "MMC API key not configured for Factiva"

  2. **scripts/test_pipeline.py** — Update references:
     - Replace `from app.services.collector import ApifyCollector` with nothing (pipeline no longer needs it)
     - Remove `collector = ApifyCollector(...)` line
     - Update PipelineOrchestrator instantiation to match new signature (no collector param)
     - Remove `is_apify_configured()` check
     - Update description comment from "Requires live Apify" to "Requires MMC API key for Factiva"
     - Update configuration check print lines

  3. **scripts/seed_sources.py** — This seeds the sources table with Apify source definitions. The sources table and Source model are kept for future extensibility, but the seed data references Apify-specific values:
     - Update script description to note sources are historical/for future use
     - Keep the seeding logic functional (Source model still exists)
     - No structural changes needed — the script can remain as reference data
     - Add a comment at top noting these sources are no longer used by the pipeline (Factiva is sole source)

  4. **scripts/validate_production_ready.py** — Remove Apify from validation checks:
     - Remove 'APIFY_TOKEN' from the environment variable check list
     - Remove 'apify_client' from the library import check list
     - Add a note in the output about Factiva being the sole collection source
  </action>
  <verify>
  Run: `python -c "import scripts.test_pipeline"` or verify the file has no syntax errors with `python -m py_compile scripts/test_pipeline.py`
  Grep for "ApifyCollector" in scripts/ directory — must return zero matches (except seed_sources.py which references Source model data, not the class).
  Grep for "is_apify_configured" in scripts/ directory — must return zero matches.
  </verify>
  <done>
  Test scripts updated to use FactivaCollector instead of ApifyCollector. Validation script no longer checks for APIFY_TOKEN or apify-client. Seed sources script annotated as historical reference. No broken imports in scripts directory.
  </done>
</task>

</tasks>

<verification>
1. `test -f app/services/collector.py` returns false (file deleted)
2. `ls app/services/sources/` shows only `__init__.py`, `base.py`, and optionally `__pycache__/`
3. `python -c "from app.services.sources import NewsSource; print('OK')"` succeeds
4. `python -c "from app.services.pipeline import PipelineOrchestrator; print('OK')"` still succeeds (Plan 01 already removed the import)
5. Zero matches for "ApifyCollector" across all .py files in app/ directory
6. Zero matches for "from app.services.collector" across all .py files
7. base.py still contains `class NewsSource(ABC)` interface
</verification>

<success_criteria>
ApifyCollector and all 6 source implementation files deleted. Sources module retains only base.py abstract interface. Scripts updated to reference FactivaCollector instead of ApifyCollector. No broken imports anywhere in the project.
</success_criteria>

<output>
After completion, create `.planning/phases/15-pipeline-simplification-cleanup/15-02-SUMMARY.md`
</output>
