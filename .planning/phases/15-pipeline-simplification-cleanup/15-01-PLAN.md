---
phase: 15-pipeline-simplification-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/pipeline.py
  - app/main.py
  - app/routers/admin.py
  - app/services/__init__.py
autonomous: true

must_haves:
  truths:
    - "Pipeline orchestrator calls FactivaCollector.collect() directly with no Apify fallback path"
    - "When Factiva collection fails after retry, pipeline skips daily brief and sends admin alert (no fallback to alternative source)"
    - "When Factiva returns zero articles, pipeline sends empty brief so recipients know system ran"
    - "Pipeline still creates Run record, deduplicates (URL + semantic), classifies, and delivers emails"
    - "PipelineOrchestrator no longer requires ApifyCollector in its constructor"
  artifacts:
    - path: "app/services/pipeline.py"
      provides: "Factiva-only pipeline orchestration with article storage"
      contains: "FactivaCollector"
    - path: "app/main.py"
      provides: "CLI pipeline entry point without ApifyCollector"
    - path: "app/routers/admin.py"
      provides: "Admin trigger routes without ApifyCollector dependency"
    - path: "app/services/__init__.py"
      provides: "Clean services module without ApifyCollector export"
  key_links:
    - from: "app/services/pipeline.py"
      to: "app/collectors/factiva.py"
      via: "FactivaCollector.collect() direct call"
      pattern: "factiva_collector\\.collect\\("
    - from: "app/services/pipeline.py"
      to: "app/services/pipeline.py"
      via: "Inline article storage (extracted from ApifyCollector.store_factiva_articles)"
      pattern: "def _store_articles"
---

<objective>
Refactor the pipeline orchestrator to use FactivaCollector as the sole collection path, removing all Apify fallback logic. Extract the article storage logic (Run creation + NewsArticle insertion) from ApifyCollector into the pipeline so ApifyCollector is no longer needed. Update all entry points (CLI, admin triggers) to stop instantiating ApifyCollector.

Purpose: PIPE-01 and PIPE-04 — make Factiva the only collection source and remove all fallback code paths. This is the prerequisite for deleting ApifyCollector in Plan 02.

Output: Pipeline.py with Factiva-only collection, main.py and admin.py without ApifyCollector, services/__init__.py cleaned.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-pipeline-simplification-cleanup/15-CONTEXT.md
@.planning/phases/15-pipeline-simplification-cleanup/15-RESEARCH.md
@.planning/phases/14-factivacollector-port/14-02-SUMMARY.md
@app/services/pipeline.py
@app/services/collector.py
@app/main.py
@app/routers/admin.py
@app/services/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor pipeline orchestrator to Factiva-only collection with inline article storage</name>
  <files>app/services/pipeline.py, app/services/__init__.py</files>
  <action>
  In app/services/pipeline.py:

  1. **Remove ApifyCollector import and INSURANCE_FALLBACK_SOURCES constant:**
     - Delete: `from app.services.collector import ApifyCollector`
     - Delete: The entire `INSURANCE_FALLBACK_SOURCES` list (lines 36-41)

  2. **Add article storage method to PipelineOrchestrator class** (extracted from ApifyCollector._store_articles):
     ```python
     def _store_articles(self, db: Session, run_id: int, articles: list) -> None:
         """Store collected articles in database.

         Args:
             db: Database session
             run_id: ID of current pipeline run
             articles: List of normalized article dicts from FactivaCollector
         """
         for article_data in articles:
             article = NewsArticle(
                 run_id=run_id,
                 title=article_data["title"],
                 description=article_data.get("description"),
                 source_url=article_data.get("url"),
                 source_name=article_data["source_name"],
                 published_at=article_data.get("published_at"),
                 collector_source=article_data.get("collector_source", "Factiva"),
                 roles=None,
                 priority=None,
                 summary=None,
                 sentiment=None,
             )
             db.add(article)
         db.commit()
     ```
     Note: Default collector_source changes from "Apify/RSS" to "Factiva" since Factiva is the only source now.

  3. **Update PipelineOrchestrator.__init__** — remove `collector: ApifyCollector` parameter:
     ```python
     def __init__(
         self,
         classifier: RoleClassificationService,
         reporter: RoleReportService,
         token_manager: Optional[TokenManager] = None
     ):
     ```
     Remove `self.collector = collector` line. Update docstring to remove ApifyCollector reference.

  4. **Refactor run_full_pipeline() Step 1** — replace the entire Factiva-try/Apify-fallback block with direct Factiva collection:
     - Remove `factiva_used`, `collection_source = "Apify/RSS"` tracking variables — collection_source is always "Factiva"
     - Remove the outer `if factiva_collector.is_configured():` / inner try/except/fallback pattern
     - Replace with direct collection flow:
       a. Create Run record at start of Step 1
       b. Call FactivaCollector.collect(query_params)
       c. URL-dedup (keep existing logic)
       d. Semantic dedup (keep existing logic)
       e. Store articles using new self._store_articles()
       f. Update Run record with article count
     - On Factiva failure: log error, update Run as FAILED, set result status to "failed", call _send_admin_alert. NO fallback.
     - When Factiva returns zero articles: set articles_collected=0, continue pipeline (generates empty brief)
     - Remove the entire `if not factiva_used:` fallback block
     - Remove `self.collector.store_factiva_articles()` call (replaced by self._store_articles)
     - Remove the post-collection `latest_run = db.query(Run).order_by(Run.id.desc()).first()` pattern — use the Run object created at start of Step 1 directly
     - Set result["collection_source"] = "Factiva" (always)

  5. **Refactor run_full_pipeline_with_email() Step 1** — same changes as step 4 above. This method has the same Factiva-try/Apify-fallback pattern that must be collapsed to direct Factiva collection. Apply identical changes.

  6. **Update run_full_pipeline() result dict default** — change `"collection_source": "Apify/RSS"` to `"collection_source": "Factiva"`.

  7. **Update run_full_pipeline_with_email() result dict default** — same change.

  8. **Update factiva.py docstring** — remove the comment "The caller (pipeline) is responsible for handling the fallback to Apify" from the collect() method docstring. Replace with "The caller (pipeline) handles retry failure by skipping the daily brief and alerting admin."

  In app/services/__init__.py:
  - Remove `from app.services.collector import ApifyCollector`
  - Remove `"ApifyCollector"` from `__all__`
  - Keep the file but update it to export nothing or only future services

  IMPORTANT: Do NOT delete the `collector_source` field from NewsArticle model or `collector_source` column from database. It's still used to identify article provenance — all new articles just get "Factiva" value.
  </action>
  <verify>
  Run: `python -c "from app.services.pipeline import PipelineOrchestrator; print('OK')"` — must import without error.
  Run: `python -c "from app.services import *; print('OK')"` — must import without referencing ApifyCollector.
  Grep for "ApifyCollector" in pipeline.py — must return zero matches.
  Grep for "INSURANCE_FALLBACK_SOURCES" in pipeline.py — must return zero matches.
  Grep for "collect_from_sources" in pipeline.py — must return zero matches.
  Grep for "fallback" (case-insensitive) in pipeline.py — must return zero matches EXCEPT in the _send_with_fallback email method (email fallback is kept, only Apify fallback removed).
  Grep for "self.collector" in pipeline.py — must return zero matches.
  </verify>
  <done>
  PipelineOrchestrator uses FactivaCollector as sole collection path. No ApifyCollector import, no fallback to Apify/RSS, no INSURANCE_FALLBACK_SOURCES. Article storage is handled inline. Collection failure triggers admin alert instead of fallback. Zero Factiva articles produces empty brief.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLI and admin entry points to remove ApifyCollector dependency</name>
  <files>app/main.py, app/routers/admin.py</files>
  <action>
  In app/main.py:

  1. **CLI pipeline entry point** (`if __name__ == "__main__"` block, around line 309):
     - Remove: `from app.services.collector import ApifyCollector`
     - Remove: `collector = ApifyCollector(apify_token=settings.apify_token)` line
     - Update PipelineOrchestrator instantiation to not pass collector:
       ```python
       orchestrator = PipelineOrchestrator(
           classifier=classifier,
           reporter=reporter,
           token_manager=token_manager
       )
       ```

  2. **Health check endpoint** (around line 209-217):
     - Remove the Apify configuration check block:
       ```python
       # Remove this entire block:
       if settings.is_apify_configured():
           checks["external_services"]["apify"] = { ... }
       else:
           checks["external_services"]["apify"] = { ... }
       ```
     - Update the MMC API Key message: change `"Factiva/equity will use fallback sources"` to `"Factiva news and equity price APIs require MMC API key"`

  3. **Startup migration** (collector_source DEFAULT):
     - The existing migration `ALTER TABLE news_articles ADD COLUMN collector_source TEXT DEFAULT 'Apify/RSS'` can remain as-is — it's idempotent and only affects old databases. New articles all get "Factiva" from the pipeline code. Do NOT change this migration to avoid breaking existing databases.

  In app/routers/admin.py:

  1. **Remove ApifyCollector import:**
     - Delete: `from app.services.collector import ApifyCollector`

  2. **Update trigger-pipeline route** (around line 288-310):
     - Remove `is_apify_configured()` check and its HTTPException
     - Remove `collector = ApifyCollector(apify_token=settings.apify_token)` line
     - Update PipelineOrchestrator instantiation to not pass collector
     - The route should now only check that Azure OpenAI is configured (classification still needed)

  3. **Update trigger-pipeline-email route** (around line 394-420):
     - Same changes as trigger-pipeline route above

  4. **Run history display** (around line 203-213):
     - Keep the source_breakdown query — it still shows per-run source counts from collector_source column
     - Update the default key: change `(src or 'Apify/RSS')` to `(src or 'Factiva')` in the source_breakdown dict comprehension

  5. **Source form CRUD routes** — Leave as-is for now. Phase 16 handles source management UI changes. The sources table and Source model are generic infrastructure kept for future extensibility per CONTEXT.md.
  </action>
  <verify>
  Run: `python -c "from app.main import app; print('OK')"` — must import without error.
  Run: `python -c "from app.routers.admin import router; print('OK')"` — must import without error.
  Grep for "ApifyCollector" in app/main.py — must return zero matches.
  Grep for "ApifyCollector" in app/routers/admin.py — must return zero matches.
  Grep for "is_apify_configured" in app/main.py — must return zero matches.
  Grep for "is_apify_configured" in app/routers/admin.py — must return zero matches.
  </verify>
  <done>
  CLI entry point and admin trigger routes instantiate PipelineOrchestrator without ApifyCollector. No Apify configuration checks remain in main.py or admin.py. Health check no longer reports Apify status. Run history defaults to "Factiva" source label.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from app.services.pipeline import PipelineOrchestrator; print('Pipeline OK')"` succeeds
2. `python -c "from app.main import app; print('Main OK')"` succeeds
3. `python -c "from app.routers.admin import router; print('Admin OK')"` succeeds
4. Zero matches for "ApifyCollector" in pipeline.py, main.py, admin.py
5. Zero matches for "INSURANCE_FALLBACK_SOURCES" in pipeline.py
6. Zero matches for "collect_from_sources" in pipeline.py
7. Zero matches for "is_apify_configured" in main.py and admin.py
8. FactivaCollector import still present in pipeline.py (it's the sole collector now)
9. Email fallback (_send_with_fallback) still works (enterprise -> Graph API fallback is separate from news source fallback)
</verification>

<success_criteria>
Pipeline orchestrator uses FactivaCollector as sole collection path. No ApifyCollector dependency anywhere in the pipeline, CLI, or admin routes. Article storage extracted into pipeline. Apify fallback logic completely removed. Admin alert sent on collection failure instead of falling back.
</success_criteria>

<output>
After completion, create `.planning/phases/15-pipeline-simplification-cleanup/15-01-SUMMARY.md`
</output>
