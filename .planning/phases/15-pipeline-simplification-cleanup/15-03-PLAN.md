---
phase: 15-pipeline-simplification-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - requirements.txt
  - .env.example
  - app/config.py
  - app/collectors/factiva.py
  - app/models/source.py
  - app/models/news_article.py
  - app/schemas/admin.py
autonomous: true

must_haves:
  truths:
    - "apify-client and feedparser are removed from requirements.txt"
    - "APIFY_TOKEN and Apify configuration section removed from .env.example"
    - "is_apify_configured() method and apify_token setting removed from config.py"
    - "No stale Apify references remain in comments across app/ directory Python files"
    - "Factiva collector docstring no longer references Apify fallback"
  artifacts:
    - path: "requirements.txt"
      provides: "Clean dependency list without Apify/RSS libraries"
    - path: ".env.example"
      provides: "Environment template without Apify variables"
    - path: "app/config.py"
      provides: "Settings class without apify_token or is_apify_configured"
  key_links:
    - from: "requirements.txt"
      to: "pip install"
      via: "dependency declaration"
      pattern: "apify-client|feedparser"
---

<objective>
Remove all Apify-related dependencies, configuration, and stale comment references from the codebase. This is the final cleanup sweep that ensures no Apify traces remain.

Purpose: CLEAN-02, CLEAN-03, and comment cleanup — remove external references to Apify infrastructure.

Output: Clean requirements.txt, .env.example, config.py, and zero Apify references in comments across app/ Python files.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-pipeline-simplification-cleanup/15-CONTEXT.md
@.planning/phases/15-pipeline-simplification-cleanup/15-RESEARCH.md
@.planning/phases/15-pipeline-simplification-cleanup/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Apify dependencies and configuration</name>
  <files>requirements.txt, .env.example, app/config.py</files>
  <action>
  1. **requirements.txt** — Remove two dependency lines:
     - Delete the `apify-client` line (line 20) and its `# Web scraping` comment header (line 19)
     - Delete the `feedparser==6.0.12` line (line 34) and its `# RSS feed parsing` comment header (line 33)
     - Keep `httpx` (still used by FactivaCollector and other clients) — move it under a more appropriate comment like `# HTTP client` if its current `# Web scraping` header is being removed

  2. **.env.example** — Remove the entire APIFY CONFIGURATION block (lines 92-105):
     ```
     # APIFY CONFIGURATION (Web Scraping)
     # ...
     APIFY_TOKEN=your_apify_token_here
     ```
     Remove the full block including the section separator comment, description, and the APIFY_TOKEN variable.

  3. **app/config.py** — Remove Apify settings:
     - Delete the `apify_token: str = ""` field (line 40, under `# Apify (for web scraping)` comment)
     - Delete the `# Apify (for web scraping)` comment (line 39)
     - Delete the `is_apify_configured()` method (lines 104-106)
     - Update the `# Web scraping` section comment to remove — the httpx dependency is already covered by MMC Core API section
  </action>
  <verify>
  Grep for "apify" (case-insensitive) in requirements.txt — must return zero matches.
  Grep for "feedparser" in requirements.txt — must return zero matches.
  Grep for "APIFY" in .env.example — must return zero matches.
  Grep for "apify" in app/config.py — must return zero matches.
  Run: `python -c "from app.config import get_settings; s = get_settings(); print('OK')"` — must succeed without apify_token field.
  Verify httpx is still in requirements.txt.
  </verify>
  <done>
  apify-client and feedparser removed from requirements.txt. APIFY_TOKEN section removed from .env.example. apify_token setting and is_apify_configured() removed from config.py. httpx retained for FactivaCollector.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep and clean all Apify references in comments and docstrings</name>
  <files>app/collectors/factiva.py, app/models/source.py, app/models/news_article.py, app/schemas/admin.py</files>
  <action>
  Run a full grep for `apify`, `Apify`, `APIFY`, `RSS` (case-insensitive) across all .py files in app/ directory. For each match, determine if it's a stale reference that should be updated or removed.

  Known targets from research:

  1. **app/collectors/factiva.py** (line ~110):
     - Comment: "The caller (pipeline) is responsible for handling the fallback to Apify"
     - Update to: "The caller (pipeline) handles retry failure by skipping the daily brief and alerting admin."

  2. **app/models/news_article.py** (line ~38-39):
     - Comment: `# Source attribution: "Factiva" or "Apify/RSS" (Phase 10)`
     - Update to: `# Source attribution: "Factiva" (sole source since Phase 15). Historical data may contain "Apify/RSS".`
     - Keep the column and its default — do NOT change the SQL default value to avoid migration issues

  3. **app/models/source.py** (lines ~14-15, ~31):
     - `SourceType.APIFY = "apify"` enum value — Keep this. Removing an enum value from the SourceType enum would break existing database rows that have this value. The enum is part of the ORM schema.
     - Comment `# Apify-specific actor ID` on actor_id column — Update to: `# Source-specific identifier (historical: Apify actor IDs)`

  4. **app/schemas/admin.py**:
     - `source_type: Literal["apify", "rss"]` — Keep as-is. This matches the SourceType enum and the existing database schema. Phase 16 handles admin UI changes.
     - Validation messages referencing "apify" — Keep as-is for same reason.

  5. **app/services/reporter.py** (line ~135):
     - `'collector_source': getattr(article, 'collector_source', None) or 'Apify/RSS'`
     - Update the fallback default from `'Apify/RSS'` to `'Factiva'`

  6. **app/models/api_event.py** — Check for any Apify references in comments. The NEWS_FALLBACK event type stays (it's a generic enum value that could be reused).

  After making all changes, run a final comprehensive grep to verify no stale Apify references remain in comments (except in intentionally preserved locations like SourceType enum and admin schemas).
  </action>
  <verify>
  Run comprehensive grep: `grep -rn -i "apify" app/ --include="*.py"` and verify:
  - Zero matches in pipeline.py (cleaned in Plan 01)
  - Zero matches in main.py (cleaned in Plan 01)
  - Zero matches in admin.py EXCEPT in source_type schema validations (kept for DB compat)
  - Zero matches in collector.py (deleted in Plan 02)
  - Zero matches in factiva.py (cleaned in this task)
  - Zero matches in reporter.py (cleaned in this task)
  - Only preserved matches: source.py SourceType.APIFY enum, schemas/admin.py source_type literals

  Run: `python -c "from app.models.news_article import NewsArticle; print('OK')"` — must succeed
  Run: `python -c "from app.services.reporter import RoleReportService; print('OK')"` — must succeed
  </verify>
  <done>
  All stale Apify references in comments and docstrings cleaned. Factiva.py docstring updated. Reporter fallback default updated to "Factiva". NewsArticle model comment updated. Only intentionally preserved references remain (SourceType enum for DB compatibility, admin schemas for Source CRUD).
  </done>
</task>

</tasks>

<verification>
1. `grep -rni "apify" requirements.txt` returns zero matches
2. `grep -rni "feedparser" requirements.txt` returns zero matches
3. `grep -rni "APIFY" .env.example` returns zero matches
4. `grep -rni "apify" app/config.py` returns zero matches
5. `grep -rni "apify" app/collectors/factiva.py` returns zero matches
6. `grep -rni "apify" app/services/reporter.py` returns zero matches
7. httpx still present in requirements.txt
8. sentence-transformers still present in requirements.txt
9. `python -c "from app.config import get_settings; print('OK')"` succeeds
10. All remaining "apify" matches are in SourceType enum (models/source.py) and admin schemas (schemas/admin.py) — both preserved for DB compatibility
</verification>

<success_criteria>
apify-client and feedparser removed from dependencies. APIFY_TOKEN removed from environment template. Apify settings removed from config. All stale Apify references in comments/docstrings cleaned. Only DB-compatibility references preserved (SourceType enum, admin schemas).
</success_criteria>

<output>
After completion, create `.planning/phases/15-pipeline-simplification-cleanup/15-03-SUMMARY.md`
</output>
