---
phase: 04-intelligence-report-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/reporter.py
  - app/schemas/report.py
autonomous: true

must_haves:
  truths:
    - "Articles for each role tab are filtered by role membership"
    - "Articles within each role tab are sorted by priority (Critical first, Monitor last)"
    - "Reporter service accepts all articles and produces a single unified HTML brief"
  artifacts:
    - path: "app/services/reporter.py"
      provides: "Role filtering and priority ranking logic, updated generate_role_brief signature"
      contains: "filter_articles_by_role"
    - path: "app/schemas/report.py"
      provides: "Updated ReportContext and new EditionStats schema"
      contains: "EditionStats"
  key_links:
    - from: "app/services/reporter.py"
      to: "app/schemas/report.py"
      via: "import EditionStats"
      pattern: "from app.schemas.report import"
---

<objective>
Implement role-based article filtering and priority ranking logic in the reporter service, plus update the reporter signature to produce a single unified brief (all roles in one HTML) instead of per-role briefs.

Purpose: This is the foundation for all other Phase 4 plans. The reporter must filter articles by role membership and sort by priority before any AI summaries or analytics can be layered on.

Output: Updated reporter.py with filter/sort logic and new generate_role_brief signature that takes all articles (no target_role parameter). Updated report.py schemas.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\BrasilIntel\mdinsights\.planning\PROJECT.md
@C:\BrasilIntel\mdinsights\.planning\ROADMAP.md
@C:\BrasilIntel\mdinsights\.planning\STATE.md
@C:\BrasilIntel\mdinsights\.planning\phases\04-intelligence-report-generation\04-RESEARCH.md
@C:\BrasilIntel\mdinsights\app\services\reporter.py
@C:\BrasilIntel\mdinsights\app\schemas\report.py
@C:\BrasilIntel\mdinsights\app\schemas\classification.py
@C:\BrasilIntel\mdinsights\app\models\news_article.py
@C:\BrasilIntel\mdinsights\app\services\pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pydantic schemas for report generation</name>
  <files>app/schemas/report.py</files>
  <action>
  Update app/schemas/report.py to add new schemas needed by Phase 4:

  1. Add `EditionStats` schema:
     - source_count: int (unique sources in this edition)
     - article_count: int (total articles)
     - entity_count: int = 0 (filled later by aggregator)
     - signal_count: int = 0 (filled later by what-to-watch)

  2. Keep existing `ReportContext` but remove target_role field (brief is now unified, not per-role). Update fields:
     - articles: List[dict] (prepared article dicts, not ORM objects)
     - report_date: datetime
     - company_name: str
     - edition_stats: Optional[EditionStats] = None
     - executive_summaries: Optional[dict] = None (role -> summary data, added in Plan 02)
     - sector_heatmap: Optional[list] = None (added in Plan 03)
     - entity_tracker: Optional[list] = None (added in Plan 04)
     - what_to_watch: Optional[Any] = None (added in Plan 05)
     - market_pulse: Optional[list] = None (added in Plan 06)

  Use `from typing import List, Dict, Any, Optional` and `from datetime import datetime`.
  </action>
  <verify>Run `python -c "from app.schemas.report import ReportContext, EditionStats; print('OK')"` from C:\BrasilIntel\mdinsights</verify>
  <done>EditionStats and updated ReportContext schemas importable without error</done>
</task>

<task type="auto">
  <name>Task 2: Implement role filtering, priority ranking, and updated reporter signature</name>
  <files>app/services/reporter.py</files>
  <action>
  Update app/services/reporter.py:

  1. Add a module-level PRIORITY_ORDER dict:
     ```python
     PRIORITY_ORDER = {"Critical": 0, "High": 1, "Medium": 2, "Monitor": 3}
     ```

  2. Add a static method `filter_articles_by_role(articles: List[dict], role: str) -> List[dict]`:
     - Filter prepared article dicts where `role in article['roles']`
     - Sort by priority using PRIORITY_ORDER (unknown priorities get value 4)
     - Return the sorted list

  3. Update `_prepare_articles` to include ALL Phase 3 fields in the dict:
     - Add: entities (json.loads if string, else []), impact_level, category, region, business_line
     - Keep existing fields: id, title, description, source_url, source_name, published_at, roles, priority, summary, sentiment

  4. Change `generate_role_brief` signature — remove `target_role` parameter. New signature:
     ```python
     def generate_role_brief(self, articles: List[NewsArticle], report_date: datetime, company_name: str = None) -> str
     ```
     - Prepare articles via _prepare_articles
     - Compute edition_stats: source_count = len(set(a.source_name for a in articles if a.source_name)), article_count = len(articles)
     - Build context dict with: articles, report_date, company_name, edition_stats (as dict)
     - Render template, apply premailer transform, return HTML

  5. Update `generate_all_role_briefs` to call the new signature (pass articles without target_role). Since the unified brief contains all roles via tabs, this method can now just call generate_role_brief once and return the single HTML. Change return type to `str` (single HTML string, not dict).

  6. Update `pipeline.py` call site: The pipeline currently calls `self.reporter.generate_role_brief(target_role=role, articles=classified_articles, report_date=report_date)`. Update to: `self.reporter.generate_role_brief(articles=classified_articles, report_date=report_date)`. Remove the `role` parameter from `run_full_pipeline` (default was "Brokers", no longer needed).

  IMPORTANT: Do NOT add Azure OpenAI client to reporter yet (that comes in Plan 02). Do NOT add aggregator imports yet. Keep the reporter focused on filtering/ranking/template rendering.
  </action>
  <verify>
  Run from C:\BrasilIntel\mdinsights:
  1. `python -c "from app.services.reporter import RoleReportService, PRIORITY_ORDER; print('Import OK')"`
  2. `python -c "from app.services.reporter import RoleReportService; r = RoleReportService(); articles = r.filter_articles_by_role([{'roles': ['Brokers'], 'priority': 'High'}, {'roles': ['Leadership'], 'priority': 'Critical'}], 'Brokers'); print(len(articles), articles[0]['priority'])"` should print "1 High"
  3. `python -c "from app.services.pipeline import PipelineOrchestrator; print('Pipeline import OK')"`
  </verify>
  <done>Reporter service has filter_articles_by_role method, _prepare_articles includes all Phase 3 fields, generate_role_brief accepts articles without target_role, pipeline.py updated to match new signature</done>
</task>

</tasks>

<verification>
1. `python -c "from app.schemas.report import ReportContext, EditionStats"` succeeds
2. `python -c "from app.services.reporter import RoleReportService, PRIORITY_ORDER, filter_articles_by_role"` succeeds (or the static method via class)
3. `python -c "from app.services.pipeline import PipelineOrchestrator"` succeeds
4. No import errors across the codebase
</verification>

<success_criteria>
- EditionStats schema exists with source_count, article_count fields
- ReportContext updated with optional fields for Phase 4 components
- Reporter filters articles by role and sorts by priority
- _prepare_articles includes all 14 article fields (Phase 1 + Phase 3)
- Pipeline calls reporter with new signature (no target_role)
- No regressions in existing imports
</success_criteria>

<output>
After completion, create `.planning/phases/04-intelligence-report-generation/04-01-SUMMARY.md`
</output>
