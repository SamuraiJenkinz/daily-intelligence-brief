---
phase: 13-admin-dashboard-enterprise-status
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/admin/partials/search_results.html
autonomous: true

must_haves:
  truths:
    - "The search results view shows a source badge per article indicating Factiva or Apify/RSS without additional clicks"
  artifacts:
    - path: "app/templates/admin/partials/search_results.html"
      provides: "Per-article collector_source badge inline with article metadata"
      contains: "collector_source"
  key_links:
    - from: "app/templates/admin/partials/search_results.html"
      to: "app/models/news_article.py"
      via: "article.collector_source field rendered as badge"
      pattern: "collector_source"
---

<objective>
Add per-article source attribution badges (Factiva vs Apify/RSS) to the search results view.

Purpose: Satisfies ADMN-03 — admins can identify at a glance which articles came from Factiva and which from Apify/RSS. The archive view shows report files (not individual articles), so source badges are placed in the search results where individual articles are listed. The existing pipeline runs table on the dashboard already shows per-run Factiva/Apify aggregate counts via source_breakdown badges — this completes the picture with per-article detail.

Output: Modified search_results.html partial with inline source badges matching the existing Factiva blue (#0077c8) and grey (bg-secondary) badge style from the dashboard runs table.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-admin-dashboard-enterprise-status/13-CONTEXT.md
@.planning/phases/13-admin-dashboard-enterprise-status/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add collector_source badge to search results article cards</name>
  <files>app/templates/admin/partials/search_results.html</files>
  <action>
In `app/templates/admin/partials/search_results.html`, add a source attribution badge to each article card in the metadata section (the `<div class="article-meta">` block, after the source_name span and before the priority badge).

Add the following badge inline with the existing metadata spans:

```html
{% if article.collector_source == 'Factiva' %}
<span class="badge me-3" style="background-color: #0077c8; font-size: 0.7rem;">
    <i class="bi bi-newspaper me-1"></i>Factiva
</span>
{% elif article.collector_source %}
<span class="badge bg-secondary me-3" style="font-size: 0.7rem;">
    <i class="bi bi-rss me-1"></i>{{ article.collector_source }}
</span>
{% endif %}
```

**Placement:** Insert AFTER the `<span class="me-3"><i class="bi bi-globe me-1"></i>{{ article.source_name or 'Unknown source' }}</span>` line and BEFORE the `{% if article.priority %}` block.

**Style rationale:** The badge colors match the exact pattern from the dashboard runs table (dashboard.html lines 202-213) — Marsh blue `#0077c8` for Factiva, Bootstrap `bg-secondary` for Apify/RSS. Font size 0.7rem keeps it subordinate to the article title.

**Edge cases:**
- Articles with `collector_source = None` (pre-Phase-10 articles that never had the field set): no badge shown. The `{% elif article.collector_source %}` check handles this — None is falsy.
- Articles with `collector_source = "Apify/RSS"`: shows grey badge with RSS icon and the text "Apify/RSS".

No changes to any Python route needed — the `article` objects passed to the template already have the `collector_source` field from the `NewsArticle` model (added in Phase 10). The search route at `admin.py:1263` passes full `NewsArticle` ORM objects via `ArticleSearchService.search()`.
  </action>
  <verify>
Start the FastAPI server and:
1. Visit `/admin/search` and search for any keyword that returns results
2. Each article card should show a Factiva (blue) or Apify/RSS (grey) badge inline with the metadata
3. Pre-Phase-10 articles (collector_source is None) should show no source badge
4. The badge font size and color match the dashboard runs table source badges
5. HTMX partial refresh (typing in search box) preserves the badges
  </verify>
  <done>
Search results display a per-article source badge (Factiva blue or Apify/RSS grey) inline in the article metadata row, matching the existing dashboard source badge style. No badge shown for articles without collector_source data.
  </done>
</task>

</tasks>

<verification>
1. Start FastAPI server: `cd C:\BrasilIntel\MDInsights && python -m uvicorn app.main:app --host 0.0.0.0 --port 8001`
2. Visit `/admin/search`, enter a keyword that returns articles
3. Verify Factiva articles show blue badge, Apify/RSS articles show grey badge
4. Verify old articles (pre-Phase-10) show no badge
5. No template rendering errors
</verification>

<success_criteria>
- ADMN-03: Per-article source attribution visible in search results without additional clicks
- Badge style consistent with existing Factiva/Apify badges in dashboard runs table
- No existing search functionality broken
- Handles None collector_source gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-dashboard-enterprise-status/13-02-SUMMARY.md`
</output>
