# MDInsights Python Dependencies

# Core FastAPI and web framework
fastapi==0.115.0
uvicorn[standard]
python-multipart

# Database ORM
sqlalchemy

# Data validation and settings
pydantic==2.11.0
pydantic-settings

# Azure SDKs
openai==2.16.0
azure-identity

# HTTP client
httpx

# HTML templating and email styling
jinja2
premailer

# Environment and configuration
python-dotenv

# Structured logging
structlog

# Deduplication (semantic similarity)
sentence-transformers>=5.0.0

# Retry logic with exponential backoff
tenacity

# Number to words conversion (for TTS preprocessing)
num2words>=0.5.13

# ElevenLabs TTS (fallback provider for audio generation)
elevenlabs

# Statistical analysis (drift detection)
scipy
